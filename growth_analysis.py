# -*- coding: utf-8 -*-
"""Growth Analysis

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1iSbJ3GUR15ZqndxUYIjJvcNWT9buy88M

**“Business Growth Analysis: Forecast Repeat Order & Referral to Revenue”**

Analisis ini berfokus pada **North Star Metrics perusahaan, yaitu Repeat Order (Kepuasaan pelanggan) dan Referral (sumber leads) sebagai pendorong utama revenue**. Notebook ini menyajikan eksplorasi data, visualisasi, serta breakdown untuk memahami pola pertumbuhan bisnis.

**Key Insisght Strategis:**

Analisis ini berfokus pada North Star Metrics perusahaan, yaitu Repeat Order (kepuasan pelanggan) dan Referral (sumber leads) sebagai pendorong utama revenue. Temuan kunci menyoroti tren, tantangan, dan peluang di kedua area penting ini.

**1. Performa Repeat Order (Kepuasan Pelanggan): Mengkhawatirkan dan Membutuhkan Intervensi Cepat**

Penurunan Tajam Sejak 2024: Semua metrik terkait Repeat Order (jumlah leads, leads unik, leads closing, dan terutama total revenue) menunjukkan penurunan signifikan dan konsisten sejak tahun 2024. Penurunan revenue mencapai -72.87% di 2024 dan berlanjut di tahun-tahun berikutnya, menandakan masalah fundamental dalam retensi dan kepuasan pelanggan.

- Indikasi Loyalitas Menurun: Penurunan jumlah leads unik dan leads closing
mengindikasikan kesulitan dalam mempertahankan klien serta mengubah minat klien lama menjadi transaksi sukses. Hal ini berpotensi merusak reputasi dan jaringan rekomendasi.

- Bottleneck 'Kirim Proposal': Sekitar 40% dari leads Repeat Order berhenti di status 'Kirim Proposal', menunjukkan bahwa proses pasca-penawaran adalah area kritis yang belum optimal.

- Kategori Layanan Pilar Utama: Meskipun secara keseluruhan menurun, 'Imigrasi & TKA' masih menjadi kontributor revenue Repeat Order terbesar (Rp 974 juta), menegaskan perannya sebagai pilar keuangan dari basis klien setia. Namun, potensi konversi masih perlu dimaksimalkan.

**Rekomendasi Strategis untuk Repeat Order:**

- Analisis Akar Masalah: Selidiki perubahan kebijakan, produk, atau layanan yang mungkin mempengaruhi kepuasan dan loyalitas sejak 2024.
-  Perkuat Program Retensi: Implementasikan program loyalitas yang memberikan nilai tambah nyata (diskon progresif, konsultasi gratis untuk kasus baru, akses prioritas, dedicated relationship manager).
- Optimalkan Proses 'Kirim Proposal': Lakukan follow-up proaktif, analisis mengapa leads berhenti di tahap ini, perkuat value proposition dalam proposal, dan pertimbangkan insentif waktu terbatas.
Edukasi Klien: Sosialisasikan spektrum layanan firma secara lengkap kepada klien lama.

**2. Performa Referral (Sumber Leads): Penurunan yang Memerlukan Revitalisasi**

- Tren Negatif Konsisten: Jumlah leads Referral, leads unik, dan leads closing dari Referral menunjukkan penurunan yang signifikan dan konsisten dari tahun ke tahun sejak 2022. Revenue dari Referral juga menunjukkan tren negatif, dengan penurunan tertinggi pada tahun 2024 (-51%).

- Konversi Belum Optimal: Sama seperti Repeat Order, 'Kirim Proposal' adalah bottleneck utama bagi leads Referral, dengan 53.7% leads berada di status ini, menunjukkan potensi konversi yang belum termanfaatkan.

- Peran Penting Referrer Spesifik: 'Referral RT' adalah referrer dominan baik dari segi jumlah closing (29) maupun revenue (Rp 678 juta), diikuti oleh 'Referral BPL', 'Unknown Referrer', dan 'Referral Stefa'. Keberadaan 'Unknown Referrer' yang performanya baik menandakan hilangnya peluang untuk membangun hubungan strategis.

- Kategori Layanan Berpotensi: 'Family Law' dan 'Lainnya/Misc' mendominasi revenue dari Referral, sementara 'Imigrasi & TKA' memiliki leads bernilai tinggi namun dengan tingkat konversi yang masih rendah (20%).

**Rekomendasi Strategis untuk Referral:**

- Perkuat Hubungan Referrer Kunci: Berikan apresiasi dan insentif yang lebih terstruktur kepada referrer berkinerja tinggi seperti 'Referral RT'.
Identifikasi dan Analisis 'Unknown Referrer': Prioritaskan untuk mengidentifikasi dan membangun hubungan dengan sumber leads 'Unknown Referrer' yang terbukti berkualitas.
-  Optimalkan Proses Pasca-Proposal: Fokus pada follow-up proaktif, peninjauan proposal, dan penanganan keberatan untuk leads Referral yang tertahan di status 'Kirim Proposal'.
- Survei dan Umpan Balik: Kumpulkan umpan balik dari referrer dan klien yang berasal dari referral untuk memahami kualitas dan efektivitas proses referral.

**3. Perbandingan Kinerja Kategori Leads Secara Keseluruhan:**

- Pentingnya Repeat Order dan Referral: Meskipun mengalami penurunan, Repeat Order dan Referral memiliki tingkat konversi yang relatif tinggi dan kontribusi revenue yang signifikan, menegaskan bahwa kedua kategori ini adalah tulang punggung pertumbuhan.
- Tingkat Konversi: Repeat Order dan Referral umumnya menunjukkan tingkat konversi yang lebih baik dibandingkan New Prospect atau New Organic, meskipun ada ruang untuk peningkatan.
-  Rata-rata Revenue per Closing Lead: Analisis ini dapat membantu mengidentifikasi kategori leads mana yang menghasilkan nilai tertinggi per transaksi sukses, sehingga alokasi sumber daya dapat lebih difokuskan pada leads dengan potensi revenue tinggi.
"""

import pandas as pd
import numpy as nop
import seaborn as sns
import matplotlib.pyplot as plt

df=pd.read_csv('Data Kelompok 2.csv')

df.info()

df

df['type_of_leads_clean'].unique()

df['value_fixed'].unique()

df

df = df.drop(columns=['Unnamed: 0', 'Unnamed: 0.1', 'Unnamed: 0.2'], errors='ignore')
df.info()

df

"""#Repeat Order (Kepuasaan Pelanggan)

## Tren tahunan dan pertumbuhan Unik Ledas Repeat Order
"""

#ubah kolom date menjadi type datetime
df['dated'] = pd.to_datetime(df['dated'])

# filter berdasarkan repeat order
repeat_order_df_unique = df[df['type_of_leads_clean'] == 'Repeat Order'].copy()

# ekstrak year
repeat_order_df_unique['year'] = repeat_order_df_unique['dated'].dt.year

# group berdasarkan tahun
yearly_unique_repeat_leads = repeat_order_df_unique.groupby('year')['leads name'].nunique().reset_index(name='distinct_repeat_leads_count')

print("Number of Distinct 'Repeat Order' Leads (by Name) per Year:")
display(yearly_unique_repeat_leads)

plt.figure(figsize=(12, 6))
sns.lineplot(x='year', y='distinct_repeat_leads_count', data=yearly_unique_repeat_leads, marker='o')
plt.title("Jumlah Unik Leads 'Repeat Order' per Tahun")
plt.xlabel('Tahun')
plt.ylabel('Jumlah Unik Leads')
plt.xticks(yearly_unique_repeat_leads['year'].unique())
plt.grid(True)

# tambahkan angka
for index, row in yearly_unique_repeat_leads.iterrows():
    plt.text(row['year'], row['distinct_repeat_leads_count'] + 1,
             str(row['distinct_repeat_leads_count']),
             color='black', ha='center', va='bottom', fontsize=10)

plt.tight_layout()
plt.show()

yearly_unique_repeat_leads['growth_percentage'] = yearly_unique_repeat_leads['distinct_repeat_leads_count'].pct_change() * 100

print("Yearly Distinct 'Repeat Order' Leads Growth:")
display(yearly_unique_repeat_leads)

plt.figure(figsize=(12, 6))
sns.lineplot(x='year', y='growth_percentage', data=yearly_unique_repeat_leads, marker='o')
plt.title('Persentase Pertumbuhan Jumlah Unik Leads Repeat Order per Tahun')
plt.xlabel('Tahun')
plt.ylabel('Pertumbuhan Persentase (%)')
plt.xticks(yearly_unique_repeat_leads['year'].unique())
plt.grid(True)
plt.tight_layout()

# tambahkan persentase
ax = plt.gca()
for i, row in yearly_unique_repeat_leads.iterrows():
    # Only label if there is a growth percentage (i.e., not the first year's NaN or 0)
    if pd.notna(row['growth_percentage']) and row['growth_percentage'] != 0:
        ax.annotate(f'{row['growth_percentage']:.1f}%',
                    (row['year'], row['growth_percentage']),
                    ha='center', va='bottom',
                    xytext=(0, 5),
                    textcoords='offset points',
                    fontsize=10,
                    color='black')
plt.show()

"""- penurunan loyalitas klien unik, artinya ada kesulitan untuk mempertahankan klien untuk menggunakan jasa yang ditawarkan
- Penurunan menunjukan adanya indikasi kurang baik terkiat kepuasaan pelanggan jangka panjang
- adanya ancaman terhadap jaringan dan reputasi, kehilangan klien unik dapat mengurangi basis klien setia dan dapat menurunkan reputasi serta rekomendasi pada orang lain

**Rekkomendasi**:
- edukasi klien tentang spektrum layanan firma yang lengkap
- berikan status klien prioritas, untuk klien yang sudah beberapa kali menggunakan jasa, dan berikan keuntungan tertentu seperti diskon untuk jasa hukum di masa depan, konsultasi awal gratis untuk jenis masalah baru, akses prioritas ke seminar hukum eksklusif, atau pengelola hubungan klien khusus.

###  Tren tahunan dan pertumbuhan  Unik Leads 'Repeat Order' dengan Status 'Closing' per Tahun
"""

closing_unique_repeat_leads = df[(df['type_of_leads_clean'] == 'Repeat Order') &
                             (df['status_category'] == 'Closing')].copy()

closing_unique_repeat_leads['year'] = closing_unique_repeat_leads['dated'].dt.year

yearly_unique_closing_repeat_leads = closing_unique_repeat_leads.groupby('year')['leads name'].nunique().reset_index(name='distinct_closing_leads_count')

print(" Number of 'Repeat Order' Leads with  'Closing' Status per Year:")
display(yearly_unique_closing_repeat_leads)

plt.figure(figsize=(12, 6))
sns.lineplot(x='year', y='distinct_closing_leads_count', data=yearly_unique_closing_repeat_leads, marker='o')
plt.title('Jumlah Unik Leads \'Repeat Order\' dengan Status Closing per Tahun')
plt.xlabel('Tahun')
plt.ylabel('Jumlah Unik Leads')
plt.xticks(yearly_unique_closing_repeat_leads['year'].unique())
plt.grid(True)

# menambahkan keternagan angka
for index, row in yearly_unique_closing_repeat_leads.iterrows():
    plt.text(row['year'], row['distinct_closing_leads_count'] + 0.1,
             str(row['distinct_closing_leads_count']),
             color='black', ha='center', va='bottom', fontsize=10)

plt.tight_layout()
plt.show()

yearly_unique_closing_repeat_leads['growth_percentage'] = yearly_unique_closing_repeat_leads['distinct_closing_leads_count'].pct_change() * 100
yearly_unique_closing_repeat_leads['growth_percentage'] = yearly_unique_closing_repeat_leads['growth_percentage'].fillna(0)

print("Yearly Growth Unique'Repeat Order' Leads with 'Closing' Status:")
display(yearly_unique_closing_repeat_leads)

plt.figure(figsize=(12, 6))
sns.lineplot(x='year', y='growth_percentage', data=yearly_unique_closing_repeat_leads, marker='o')
plt.title('Persentase Pertumbuhan Jumlah Unik Leads Repeat Order dengan Status Closing per Tahun')
plt.xlabel('Tahun')
plt.ylabel('Pertumbuhan Persentase (%)')
plt.xticks(yearly_unique_closing_repeat_leads['year'].unique())
plt.grid(True)
plt.tight_layout()

ax = plt.gca()
for i, row in yearly_unique_closing_repeat_leads.iterrows():
    if row['growth_percentage'] != 0:
        ax.annotate(f'{row['growth_percentage']:.1f}%',
                    (row['year'], row['growth_percentage']),
                    ha='center', va='bottom',
                    xytext=(0, 5),
                    textcoords='offset points',
                    fontsize=10,
                    color='black')
plt.show()

"""## Tren Tahunan dan Pertumbuhan Leads 'Repeat Order'"""

# Filter 'Repeat Order' leads
repeat_order_df_trend = df[df['type_of_leads_clean'] == 'Repeat Order'].copy()

# Ekstrak tahun
repeat_order_df_trend['year'] = repeat_order_df_trend['dated'].dt.year

# Group berdasarkan jumlah per tahun 'Repeat Order' leads
yearly_repeat_orders = repeat_order_df_trend.groupby('year').size().reset_index(name='count')

# kalkulasi persentase pertumbuhan
yearly_repeat_orders['growth_percentage'] = yearly_repeat_orders['count'].pct_change() * 100

print("Yearly Repeat Order Leads and Growth:")
display(yearly_repeat_orders)

plt.figure(figsize=(12, 6))
sns.lineplot(x='year', y='count', data=yearly_repeat_orders, marker='o')
plt.title('Jumlah Leads \'Repeat Order\' per Tahun')
plt.xlabel('Tahun')
plt.ylabel('Jumlah Leads')
plt.xticks(yearly_repeat_orders['year'].unique())
plt.grid(True)

# menambahkan keterangan angka
for index, row in yearly_repeat_orders.iterrows():
    plt.text(row['year'], row['count'] + 1,  # Adjust +1 for label position slightly above point
             str(row['count']),
             color='black', ha='center', va='bottom', fontsize=10)

plt.tight_layout()
plt.show()

plt.figure(figsize=(12, 6))
sns.lineplot(x='year', y='growth_percentage', data=yearly_repeat_orders, marker='o')
plt.title('Persentase Pertumbuhan Jumlah Leads \'Repeat Order\' per Tahun')
plt.xlabel('Tahun')
plt.ylabel('Pertumbuhan Persentase (%)')
plt.xticks(yearly_repeat_orders['year'].unique()) # Ensure all years are shown on x-axis
plt.grid(True)
plt.tight_layout()

# menambahkan ketrangan persentase
ax = plt.gca()
for i, row in yearly_repeat_orders.iterrows():
    # Only label if there is a growth percentage (i.e., not the first year's NaN or 0)
    if pd.notna(row['growth_percentage']) and row['growth_percentage'] != 0:
        ax.annotate(f'{row['growth_percentage']:.1f}%',
                    (row['year'], row['growth_percentage']),
                    ha='center', va='bottom',
                    xytext=(0, 5),
                    textcoords='offset points',
                    fontsize=10,
                    color='black')
plt.show()

"""-Trend Negatif dari 2024. Penurunan signifikan pada tahun 2024 (-38%), dan berlanjut pada tahun berikutnya 2025 (-30%)
- adanya indikasi permasalahan fundamental, perubahan kepuasaan pelanggan, strategi retensi yang tidak efferktif atau perubahan pasar

**Rekomendasi :**
1. Analisis Permasalahan : apakah ada perubahan kebijakan, produk, dan layanan perusahaan?
2. Perkuatan Retensi Pelanggan : Pemberian nilai tambah yang nyata untuk klien yang repeat order (diskon untuk layanan lanjutan, konslultasi gratis untuk permasalahan yang berbeda, akses prioritas,  komunikasi yang personal dan relevan dengan klien), pastikan layanan purna jual tetap bagus untuk menjaga kepuasaan pelanggan dan membangun hubungan jangka panjang
3. Mengimplementasikan sistem yang mengumpulkan umpan balik dari pelanggan secara proaktif

###  Tren tahunan dan pertumbuhan  Leads 'Repeat Order' dengan Status 'Closing' per Tahun
"""

# Filter 'Repeat Order' leads with 'Closing' status
closing_repeat_orders = df[(df['type_of_leads_clean'] == 'Repeat Order') & (df['status_category'] == 'Closing')].copy()

# ekstrak year
closing_repeat_orders['year'] = pd.to_datetime(closing_repeat_orders['dated']).dt.year

# Group berdasaarkan tahun status closing
yearly_closing_repeat_orders = closing_repeat_orders.groupby('year').size().reset_index(name='count_closing')

print("Yearly Closing Repeat Orders:")
display(yearly_closing_repeat_orders)

plt.figure(figsize=(12, 6))
sns.lineplot(x='year', y='count_closing', data=yearly_closing_repeat_orders, marker='o')
plt.title('Jumlah Leads \'Repeat Order\' dengan Status  closing per Tahun')
plt.xlabel('Tahun')
plt.ylabel('Jumlah Leads')
plt.xticks(yearly_repeat_orders['year'].unique())
plt.grid(True)

# menambahkan keterangan angka
for index, row in yearly_closing_repeat_orders.iterrows():
    plt.text(row['year'], row['count_closing'] + 0.1,  # Adjust +0.1 for label position slightly above point
             str(row['count_closing']),
             color='black', ha='center', va='bottom', fontsize=10)

plt.tight_layout()
plt.show()

yearly_closing_repeat_orders['growth_percentage'] = yearly_closing_repeat_orders['count_closing'].pct_change() * 100
yearly_closing_repeat_orders['growth_percentage'] = yearly_closing_repeat_orders['growth_percentage'].fillna(0) # Fill NaN for the first year with 0

print("Yearly Growth of 'Closing' Repeat Order Leads:")
display(yearly_closing_repeat_orders)

plt.figure(figsize=(12, 6))
sns.lineplot(x='year', y='growth_percentage', data=yearly_closing_repeat_orders, marker='o')
plt.title('Persentase Pertumbuhan Leads \'Repeat Order\' dengan Status Closing per Tahun')
plt.xlabel('Tahun')
plt.ylabel('Pertumbuhan Persentase (%)')
plt.xticks(yearly_closing_repeat_orders['year'].unique())
plt.grid(True)
plt.tight_layout()

# menambahkan keterangan persentase
ax = plt.gca()
for i, row in yearly_closing_repeat_orders.iterrows():
    # Only label if there is a growth percentage (i.e., not the first year's 0)
    if pd.notna(row['growth_percentage']) and row['growth_percentage'] != 0:
        ax.annotate(f'{row['growth_percentage']:.1f}%',
                    (row['year'], row['growth_percentage']),
                    ha='center', va='bottom',
                    xytext=(0, 5),
                    textcoords='offset points',
                    fontsize=10,
                    color='black')
plt.show()

"""- penurunan konversi yang sangat signifikan pada tahun 2024
- adanya permsalahan dalam kemampuan perusahaan untuk mengubahan repeat leads menjadi transaksi yang closing
- ada indikasi permasalahan kepercayaan klien, kurangnya daya saing penawaran firma, atau pengalaman closing yang kurang optimal

**Rekomendasi:**
-  identifikasi akar permasalahan : pada tahapan mana mengalami hambatan
- kaji ulang penawaran dan harga untuk klien loyal
- meningktakan kemampuan negosiasi dan penanganan keberatab tim
- pekuat portofolio keberhasilan

## Distribusi Leads Repeat Order Berdasarakan Status Category
"""

# Filter data 'Repeat Order' leads
repeat_order_df = df[df['type_of_leads_clean'] == 'Repeat Order']

# hitung distribusi  dari 'status_category' untuk 'Repeat Order' leads
status_distribution = repeat_order_df['status_category'].value_counts()

print("Distribution of 'status_category' for 'Repeat Order' leads:")
print(status_distribution)

# hitung persentase
status_percentages = status_distribution / status_distribution.sum() * 100

# visualisasi dengan bar chart
plt.figure(figsize=(10, 6))
ax = sns.barplot(x=status_percentages.index, y=status_percentages.values, palette='viridis', hue=status_percentages.index, legend=False)
plt.title('Persentase Distribusi Status Category Berdasarkan Repeat Order Leads')
plt.xlabel('Status Category')
plt.ylabel('Percentage of Leads')
plt.xticks(rotation=45, ha='right')
plt.tight_layout()

# tambah keterangan persentase
for p in ax.patches:
    ax.annotate(f'{p.get_height():.1f}%',
                (p.get_x() + p.get_width() / 2., p.get_height()),
                ha='center', va='center',
                xytext=(0, 9),
                textcoords='offset points')
plt.show()

"""- status closing dominan, artinya Repeat order memiliki tingkat konversi yang cukup kuat. Namun 49% dengan status lain menunjukan masih adanya peluang besar untuk mengoptimalkan konversi
- Potensi konversi tinggi di **Kirim Proposal** : 40% dari leads repeat order berada dalam status "kirim propoal". artinnya klien sudah menerima penawaran tetapi belum melakukan dapat mencapai tahap closing.

**Staretgi terkait Konversi pada kirim proposal**:
- Analisis Pemberhentian : Lakukan analsis untuk memahami alasan umum kenapa klien berhenti khususnya  pada tahapan kirim proposa, namun tidak menutup untuk tahapan lainnya seperti unkonwn, diskusi, dll
- Follow-up Proaktif, follow-up yang untuk membahas proposal, pertanyaan, dan mengatasi kendala terkait proposal, dengan berbagai media yang di sesuaikan dengan klien telpon, pesan, email, ataupun pertemuan langsung
- Penawaran Khusus: Memberikan penawaran khusus untuk klien yang repeat order seperti diskon, atau penawaran khusus lainnya

## Tren Tahunan dan Pertumbuhan Revenue Repeat Order
"""

#hitung jumlah revenue repeat order berdasarkan tahun
yearly_value_fixed_closing = closing_repeat_orders.groupby('year')['value_fixed'].sum().reset_index(name='total_value_fixed')

print("Yearly Total Revenue for Closing Repeat Orders:")
display(yearly_value_fixed_closing)

plt.figure(figsize=(12, 6))
sns.lineplot(x='year', y='total_value_fixed', data=yearly_value_fixed_closing, marker='o')
plt.title('Total Revenue dari Leads Repeat Order')
plt.xlabel('Tahun')
plt.ylabel('Total Value Fixed')
plt.xticks(yearly_value_fixed_closing['year'].unique())
plt.grid(True)
plt.ticklabel_format(style='plain', axis='y')

# menambahkan keterangan angka
for index, row in yearly_value_fixed_closing.iterrows():
    plt.text(row['year'], row['total_value_fixed'] + (yearly_value_fixed_closing['total_value_fixed'].max() * 0.02),
             f'Rp{row['total_value_fixed']:,}',
             color='black', ha='center', va='bottom', fontsize=10, rotation=45)

plt.tight_layout()
plt.show()

yearly_value_fixed_closing['growth_percentage'] = yearly_value_fixed_closing['total_value_fixed'].pct_change() * 100
yearly_value_fixed_closing['growth_percentage'] = yearly_value_fixed_closing['growth_percentage'].fillna(0)

print("Yearly Total 'Value_fixed' for Closing Repeat Orders with Growth:")
display(yearly_value_fixed_closing)

plt.figure(figsize=(10, 6))
sns.lineplot(x='year', y='growth_percentage', data=yearly_value_fixed_closing, marker='o')
plt.title('Persentase Pertumbuhan Revenue Repeat Order per Tahun')
plt.xlabel('Tahun')
plt.ylabel('Growth Percentage (%)')
plt.xticks(yearly_value_fixed_closing['year'].unique())
plt.grid(True)
plt.tight_layout()

# menambahkan keterangan persentase
ax = plt.gca()
for i, row in yearly_value_fixed_closing.iterrows():
    if row['growth_percentage'] != 0:
        ax.annotate(f'{row['growth_percentage']:.1f}%',
                    (row['year'], row['growth_percentage']),
                    ha='center', va='bottom',
                    xytext=(0, 5),
                    textcoords='offset points',
                    fontsize=10,
                    color='black')
plt.show()

"""- Setelah pertumbuhan yang positif di tahun 2023 (+18%), terjadi penurunan pendapatan yang sangat tajam dari klien repeat yang berhasil di-closing: -72.87% di tahun 2024, -21.79% di tahun 2025, dan puncaknya -93.16% di tahun 2026. Ini adalah indikator kritis bahwa firma mengalami masalah serius dalam mempertahankan dan mengkonversi transaksi bernilai tinggi dari basis klien setianya.
-  Klien repeat yang closing biasanya merupakan sumber pendapatan yang lebih stabil dan efisien dibandingkan akuisisi klien baru. Penurunan ini menunjukkan bahwa 'urat nadi' finansial yang penting ini sedang melemah dengan cepat.
- Kehilangan pendapatan dari klien repeat secara berkelanjutan akan berdampak negatif pada proyeksi keuangan, kemampuan investasi, dan stabilitas operasional firma. Ini mungkin juga mencerminkan ketidakmampuan untuk 'upsell' atau 'cross-sell' layanan tambahan kepada klien yang sudah ada.

**Rekomendasi:**
-  Analisis layanan yang menjadi pendrong utama penurunan pendapatan dan penyesuaiaan strategi pemasaran
- Analisis value gap: diskusi dengan klien yang maish aktif dan atau jika memungkinkan dengan yang sudah "hilang", untuk melihat layanan aktual dan layanan yang diharapkan klien
- optimalisasi strategi dan penawaran serta penerapan harga

###  Rangkuman Insight dari Repeat Order

1.  **Penurunan Signifikan pada 2024 sebagai Titik Balik Kritis:**
    *   **Jumlah Leads 'Repeat Order' (Total):** Grafik menunjukkan penurunan tajam jumlah *leads* *repeat order* sejak 2024. Ini mengindikasikan bahwa jumlah pelanggan lama yang kembali menunjukkan minat semakin berkurang.
    *   **Jumlah Unik Leads 'Repeat Order':** Penurunan drastis pada jumlah pelanggan unik yang melakukan *repeat order* sejak 2024 menyoroti masalah serius dalam mempertahankan basis pelanggan setia atau menarik kembali pelanggan lama yang berbeda.
    *   **Jumlah Leads 'Repeat Order' dengan Status 'Closing':** Penurunan jumlah *leads* *repeat order* yang berhasil di-closing juga sangat signifikan sejak 2024. Ini berarti bukan hanya jumlah *leads* yang berkurang, tetapi juga efektivitas konversi dari *leads* yang ada semakin menurun.
    *   **Total Revenue dari 'Repeat Order' dengan Status 'Closing':** Penurunan pendapatan yang sangat tajam dari *repeat order* yang di-closing sejak 2024 adalah indikator paling krusial bahwa masalah ini berdampak langsung pada performa keuangan perusahaan. Ini menunjukkan bahwa nilai transaksi dari pelanggan setia juga menurun, atau pelanggan dengan nilai transaksi tinggi tidak lagi melakukan *repeat order*.

2.  **Indikasi Permasalahan Fundamental pada Retensi dan Kepuasan Pelanggan:**
    *   Penurunan di semua metrik *repeat order* secara bersamaan (jumlah *leads*, *leads* unik, *closing*, dan *revenue*) sejak 2024 menunjukkan adanya masalah sistemik atau fundamental, bukan hanya fluktuasi sementara.
    *   Ini bisa disebabkan oleh perubahan kualitas layanan, kurangnya inovasi produk/layanan, strategi retensi yang tidak efektif, munculnya kompetitor baru yang lebih menarik, atau perubahan preferensi pasar yang tidak diantisipasi.

## Repeat Order Leads Berdasarkan Status Category dan Service Category

### Perbandingan Leads 'Repeat Order': Status 'Closing' vs. 'Status Lainnya'
"""

# Filter 'Repeat Order' leads
repeat_order_leads = df[df['type_of_leads_clean'] == 'Repeat Order'].copy()

# buat kolom untuk status category clsing dan lainnya
repeat_order_leads['status_group'] = repeat_order_leads['status_category'].apply(lambda x: 'Closing' if x == 'Closing' else 'Other Status')

# hitung distibusi repeat order berdasarkan status
status_group_distribution = repeat_order_leads['status_group'].value_counts()

print("Distribution of 'Closing' vs. 'Other Status' for Repeat Order Leads:")
display(status_group_distribution)

# hitung persentase
status_group_percentages = (status_group_distribution / status_group_distribution.sum()) * 100

# Visualisasi
plt.figure(figsize=(10, 6))
ax = sns.barplot(x=status_group_percentages.index, y=status_group_percentages.values, palette='coolwarm', hue=status_group_percentages.index, legend=False)
plt.title('Percentage Distribution of Closing vs. Other Status for Repeat Order Leads')
plt.xlabel('Status Group')
plt.ylabel('Percentage of Leads')
plt.xticks(rotation=0) # Keep labels horizontal
plt.tight_layout()

# menambahkan keterangan persentase
for p in ax.patches:
    ax.annotate(f'{p.get_height():.1f}%',
                (p.get_x() + p.get_width() / 2., p.get_height()),
                ha='center', va='center',
                xytext=(0, 9),
                textcoords='offset points',
                fontsize=10,
                color='black')
plt.show()

"""### Distribusi Status Category Closing vs Lainnya Leads Repeat order berdasarkan Service Category"""

service_closing_other_distribution = repeat_order_leads.groupby(['service_category_big', 'status_group']).size().unstack(fill_value=0)
service_closing_other_percentage = service_closing_other_distribution.apply(lambda x: x / x.sum() * 100, axis=1)

print("Distribution of 'Closing' vs. 'Other Status' for Repeat Order Leads by Service Category:")
display(service_closing_other_distribution)

print("\nPercentage Distribution:")
display(service_closing_other_percentage)

"""- imigrasi & TKA adalah Performa Terbaik: Kategori 'Imigrasi & TKA' memiliki jumlah leads repeat order yang di-closing paling tinggi (64) dan juga jumlah leads repeat order secara keseluruhan yang paling banyak (64+56 = 120 leads), menunjukkan bahwa ini adalah mesin utama untuk bisnis repeat dan kepuasan klien. namun perlu peningkatan konversi agar lebih maksimal
- potensi Optimasi di Beberapa Kategori: Meskipun beberapa kategori memiliki angka closing yang cukup, ada peluang untuk meningkatkan konversi di kategori dengan leads 'Other Status' yang signifikan (misalnya, 'Family Law', 'Corporate').
- kategori dengan Konversi Sempurna (tetapi kecil): 'Konsultasi' menunjukkan konversi 100% (1 closing, 0 other), meskipun volumenya sangat kecil. Ini bisa menjadi indikasi potensi yang belum tergali atau keberhasilan spesifik.
Kategori Bermasalah: 'Retainer' memiliki 0 closing dan 2 'Other Status', menandakan ada masalah mendasar dalam mengonversi leads repeat untuk layanan ini.
"""

# Visualize the distribution using a stacked bar chart
plt.figure(figsize=(14, 8))
ax = service_closing_other_percentage.plot(kind='bar', stacked=True, figsize=(14, 8), cmap='coolwarm')
plt.title('Persentase Distribusi Closing VS Lainnya berdsarkan service Category')
plt.xlabel('Service Category Big')
plt.ylabel('Percentage of Leads')
plt.xticks(rotation=45, ha='right')
plt.legend(title='Status Group', bbox_to_anchor=(1.05, 1), loc='upper left')

# menambahkan keternagan persentase
for container in ax.containers:
    labels = [f'{w:.1f}%' if (w := v.get_height()) > 0 else '' for v in container]
    ax.bar_label(container, labels=labels, label_type='center', fontsize=9, color='white', fontweight='bold')

plt.tight_layout()
plt.show()

"""### Distribusi Revenue Repeat Order Leads Berdasarakan Service Category"""

# Filter for 'Repeat Order' leads
repeat_order_leads_revenue = df[df['type_of_leads_clean'] == 'Repeat Order'].copy()

# Group by 'service_category_big' and sum 'value_fixed'
total_revenue_per_service = repeat_order_leads_revenue.groupby('service_category_big')['value_fixed'].sum().reset_index()

# Sort for better visualization and identification of the highest revenue
total_revenue_per_service = total_revenue_per_service.sort_values(by='value_fixed', ascending=False)

print("Total Revenue (Value_Fixed) from 'Repeat Order' Leads per Service Category:")
display(total_revenue_per_service)

# Identify the service category with the highest revenue
highest_revenue_category = total_revenue_per_service.iloc[0]
print(f"\nService Category with the Highest Revenue from Repeat Orders: {highest_revenue_category['service_category_big']} (Rp{highest_revenue_category['value_fixed']:,})")

# visualisasi
plt.figure(figsize=(14, 8))
ax = sns.barplot(x='service_category_big', y='value_fixed', data=total_revenue_per_service, palette='crest', hue='service_category_big', legend=False)
plt.title('Total Revenue dari Repeat Order Leads berdasarkan Service Category')
plt.xlabel('Service Category Big')
plt.ylabel('Total Revenue (IDR)')
plt.xticks(rotation=45, ha='right')
plt.ticklabel_format(style='plain', axis='y') # Prevent scientific notation
plt.grid(axis='y', linestyle='--', alpha=0.7)

# Add numerical labels on top of each bar
for p in ax.patches:
    ax.annotate(f'Rp{p.get_height():,.0f}',
                (p.get_x() + p.get_width() / 2., p.get_height()),
                ha='center', va='center',
                xytext=(0, 9),
                textcoords='offset points',
                fontsize=10,
                color='black')
plt.tight_layout()
plt.show()

"""- Dominasi 'Imigrasi & TKA': Kategori 'Imigrasi & TKA' secara signifikan menyumbang pendapatan terbesar (Rp 974.082.200) dari leads repeat order. Ini menegaskan bahwa kategori ini adalah pilar keuangan utama dari basis klien setia Anda.
- Kontributor Menengah: 'Lainnya/Misc' (Rp 374.950.000) dan 'Family Law' (Rp 155.500.000) juga merupakan kontributor pendapatan repeat yang substansial, menunjukkan bahwa ada loyalitas klien yang baik di area ini.
Kontributor Kecil: 'Corporate' (Rp 81.500.000) dan 'Litigasi' (Rp 30.000.000) menyumbang lebih sedikit, meskipun masih signifikan.
- pendapatan Minimal/Nol: 'Konsultasi' hanya menyumbang Rp 600.000 dan 'Retainer' menyumbang Rp 0. Ini menunjukkan bahwa meskipun ada repeat leads, kategori ini tidak efektif dalam menghasilkan pendapatan dari klien repeat, atau volume kasusnya sangat rendah.

### Detail Distribusi Status Category Leads Repeat Order Berdasarkan Service Category
"""

# Group berdasarkan  'service_category_big' dan 'status_category'
service_status_distribution = repeat_order_leads.groupby(['service_category_big', 'status_category']).size().unstack(fill_value=0)

# hitung persentase
service_status_percentage = service_status_distribution.apply(lambda x: x / x.sum() * 100, axis=1)

print("Distribution of Status Category for 'Repeat Order' Leads by Service Category:")
display(service_status_distribution)

print("\nPercentage Distribution:")
display(service_status_percentage)

"""- imigrasi & TKA Unggul dalam Konversi: Kategori 'Imigrasi & TKA' menunjukkan jumlah closing yang paling tinggi (64), menegaskan statusnya sebagai pilar utama pendapatan repeat order. Namun, masih ada 43 leads di tahap 'Kirim Proposal', yang menandakan potensi besar yang belum termanfaatkan.
- "Kirim Proposal" sebagai Bottleneck Utama: Hampir di semua kategori yang memiliki volume leads signifikan (Family Law, Imigrasi & TKA, Lainnya/Misc, Corporate), status 'Kirim Proposal' menjadi titik tumpuk leads terbanyak setelah 'Closing'. Ini mengindikasikan bahwa proses pasca-penawaran adalah area kritis untuk perbaikan.
- Kategori dengan Konversi Rendah/Bermasalah:
Family Law: Memiliki leads 'Kirim Proposal' (14) yang jauh lebih banyak dari 'Closing' (9), menunjukkan tantangan dalam mengkonversi penawaran.
Retainer: Tidak ada leads 'Closing' sama sekali, dengan 2 leads di 'Kirim Proposal', menunjukkan masalah fundamental dalam penawaran retainer kepada klien repeat.
- "Konsultasi" sebagai Model Konversi yang Efisien (skala kecil): Meskipun volumenya sangat kecil (1 closing), 100% konversi dari status ini perlu diperhatikan, mungkin bisa direplikasi di skala yang lebih besar.
"""

# Visualisasi
plt.figure(figsize=(14, 8))
service_status_percentage.plot(kind='bar', stacked=True, figsize=(14, 8), cmap='viridis')
plt.title('Percentage Distribution of Status Category for Repeat Order Leads by Service Category')
plt.xlabel('Service Category Big')
plt.ylabel('Percentage of Leads')
plt.xticks(rotation=45, ha='right')
plt.legend(title='Status Category', bbox_to_anchor=(1.05, 1), loc='upper left')
plt.tight_layout()
plt.show()

"""#Referral Menjadi Revenue"""

# Create a copy of the DataFrame
df_categorized = df.copy()

# Define a function to categorize 'type_of_leads_clean'
def categorize_leads(lead_type):
    if lead_type == 'Repeat Order':
        return 'Repeat Order'
    elif lead_type == 'New Prospect':
        return 'New Prospect'
    elif lead_type == 'New Organic':
        return 'New Organic'
    elif lead_type.startswith('Referral') or lead_type == 'Unknown Referrer':
        return 'Referral'
    else:
        return 'Others' # Handle any other unexpected categories

# Apply the function to create the new 'type_of_leads_category' column
df_categorized['type_of_leads_category'] = df_categorized['type_of_leads_clean'].apply(categorize_leads)

# Display the value counts of the new column to verify
print("Distribution of new 'type_of_leads_category':")
display(df_categorized['type_of_leads_category'].value_counts())

df_categorized

"""## Tren Tahunan dan Pertumbuhan Jumlah Unik Leads Referral"""

referral_df_unique = df_categorized[df_categorized['type_of_leads_category'] == 'Referral'].copy()

# ubah kolom 'dated' menjadi type datetime di df_categorized
referral_df_unique['dated'] = pd.to_datetime(referral_df_unique['dated'])

# Ekstrak tahun
referral_df_unique['year'] = referral_df_unique['dated'].dt.year

# group berdasarkan tahun dan unik leads
yearly_unique_referral_leads = referral_df_unique.groupby('year')['leads name'].nunique().reset_index(name='distinct_referral_leads_count')

print("Number of Distinct 'Referral' Leads per Year:")
display(yearly_unique_referral_leads)

plt.figure(figsize=(12, 6))
sns.lineplot(x='year', y='distinct_referral_leads_count', data=yearly_unique_referral_leads, marker='o')
plt.title('Jumlah Unik Leads \'Referral\' per Tahun')
plt.xlabel('Tahun')
plt.ylabel('Jumlah Unik Leads')
plt.xticks(yearly_unique_referral_leads['year'].unique())
plt.grid(True)

# tambahkan angka
for index, row in yearly_unique_referral_leads.iterrows():
    plt.text(row['year'], row['distinct_referral_leads_count'] + 0.1,
             str(row['distinct_referral_leads_count']),
             color='black', ha='center', va='bottom', fontsize=10)

plt.tight_layout()
plt.show()

# menghitung pertumbuhan unik leads referral berdasarkan tahun
yearly_unique_referral_leads['growth_percentage'] = yearly_unique_referral_leads['distinct_referral_leads_count'].pct_change() * 100
yearly_unique_referral_leads['growth_percentage'] = yearly_unique_referral_leads['growth_percentage'].fillna(0) # Fill NaN for the first year with 0

print("Yearly Growth of Distinct 'Referral' Leads:")
display(yearly_unique_referral_leads)

plt.figure(figsize=(12, 6))
sns.lineplot(x='year', y='growth_percentage', data=yearly_unique_referral_leads, marker='o')
plt.title('Persentase Pertumbuhan Jumlah Unik Leads Referral per Tahun')
plt.xlabel('Tahun')
plt.ylabel('Pertumbuhan Persentase (%)')
plt.xticks(yearly_unique_referral_leads['year'].unique()) # Ensure all years are shown on x-axis
plt.grid(True)
plt.tight_layout()

# Add percentage labels on top of each point
ax = plt.gca()
for i, row in yearly_unique_referral_leads.iterrows():
    if row['growth_percentage'] != 0:
        ax.annotate(f'{row['growth_percentage']:.1f}%',
                    (row['year'], row['growth_percentage']),
                    ha='center', va='bottom',
                    xytext=(0, 5),
                    textcoords='offset points',
                    fontsize=10,
                    color='black')
plt.show()

"""- Terjadi penurunan jumlah unik leads referral yang konsisten dari tahun ke tahun sejak 2022. mengindkasikan adanya penurunan peforma dan efektifitas referrer

**Rekomendasi :**
- Perkuat hubungan dengan referrer: berikan apresiasi dan insentif yang lebih  
- survei kepuasaan referrer dan klien yang bersumber dari nya
- evaluasi kualitas referral

### Tren dan Pertumbuhan Unik Leads Referral dengan Status Closing Per Tahun
"""

# Filter 'Referral' leads dengan 'Closing' status
closing_referral_leads = df_categorized[(df_categorized['type_of_leads_category'] == 'Referral') &
                                        (df_categorized['status_category'] == 'Closing')].copy()

# Ekstrak tahun dari kolom 'dated'
closing_referral_leads['year'] = pd.to_datetime(closing_referral_leads['dated']).dt.year

# Group berdasarkan tahun dan hitung referral closing
yearly_unique_closing_referral_leads = closing_referral_leads.groupby('year')['leads name'].nunique().reset_index(name='distinct_closing_referral_leads_count')

# hitung persentase pertumbuhan
yearly_unique_closing_referral_leads['growth_percentage'] = yearly_unique_closing_referral_leads['distinct_closing_referral_leads_count'].pct_change() * 100
yearly_unique_closing_referral_leads['growth_percentage'] = yearly_unique_closing_referral_leads['growth_percentage'].fillna(0) # Fill NaN for the first year with 0

print("Yearly Distinct 'Closing' Referral Leads (by Name) and Growth:")
display(yearly_unique_closing_referral_leads)

# Visualiasi
plt.figure(figsize=(12, 6))
sns.lineplot(x='year', y='distinct_closing_referral_leads_count', data=yearly_unique_closing_referral_leads, marker='o')
plt.title('Jumlah Unik Leads Referral dengan Status Closing per Tahun')
plt.xlabel('Tahun')
plt.ylabel('Jumlah Unik Leads Closing')
plt.xticks(yearly_unique_closing_referral_leads['year'].unique())
plt.grid(True)

# tambahkan angka
for index, row in yearly_unique_closing_referral_leads.iterrows():
    plt.text(row['year'], row['distinct_closing_referral_leads_count'] + 0.1,
             str(row['distinct_closing_referral_leads_count']),
             color='black', ha='center', va='bottom', fontsize=10)

plt.tight_layout()
plt.show()

# Visualisasi
plt.figure(figsize=(12, 6))
sns.lineplot(x='year', y='growth_percentage', data=yearly_unique_closing_referral_leads, marker='o')
plt.title('Persentase Pertumbuhan Jumlah Unik Leads Referral dengan Status Closing per Tahun')
plt.xlabel('Tahun')
plt.ylabel('Pertumbuhan Persentase (%)')
plt.xticks(yearly_unique_closing_referral_leads['year'].unique()) # Ensure all years are shown on x-axis
plt.grid(True)
plt.tight_layout()

# Add percentage labels on top of each point
ax = plt.gca()
for i, row in yearly_unique_closing_referral_leads.iterrows():
    if row['growth_percentage'] != 0:
        ax.annotate(f'{row['growth_percentage']:.1f}%',
                    (row['year'], row['growth_percentage']),
                    ha='center', va='bottom',
                    xytext=(0, 5),
                    textcoords='offset points',
                    fontsize=10,
                    color='black')
plt.show()

"""- penrunan drastis konversi leads yang bersumber dari referral tertinggi pada tahun 2024 (75%)

**Rekomendasi:**

- Analisis Kualitas Referrer
- optimalisasi proses konversi
- survei dan umpan balik

## Tren Tahunan dan Pertumbuhan Leads Referral
"""

# Filter 'Referral' leads
referral_df_trend = df_categorized[df_categorized['type_of_leads_category'] == 'Referral'].copy()

# Ubah kolom 'dated' menjadi type datetime
referral_df_trend['dated'] = pd.to_datetime(referral_df_trend['dated'])

# Ekstrak year
referral_df_trend['year'] = referral_df_trend['dated'].dt.year

# Group berdasarkan tahun
yearly_referral_leads = referral_df_trend.groupby('year').size().reset_index(name='count')

# hitung persentase pertumbuhan
yearly_referral_leads['growth_percentage'] = yearly_referral_leads['count'].pct_change() * 100
yearly_referral_leads['growth_percentage'] = yearly_referral_leads['growth_percentage'].fillna(0) # Fill NaN for the first year with 0

print("Yearly Referral Leads and Growth:")
display(yearly_referral_leads)

plt.figure(figsize=(12, 6))
sns.lineplot(x='year', y='count', data=yearly_referral_leads, marker='o')
plt.title('Jumlah Leads \'Referral\' per Tahun')
plt.xlabel('Tahun')
plt.ylabel('Jumlah Leads')
plt.xticks(yearly_referral_leads['year'].unique())
plt.grid(True)

# tambahkan keterangan angka
for index, row in yearly_referral_leads.iterrows():
    plt.text(row['year'], row['count'] + 1,
             str(row['count']),
             color='black', ha='center', va='bottom', fontsize=10)

plt.tight_layout()
plt.show()

plt.figure(figsize=(12, 6))
sns.lineplot(x='year', y='growth_percentage', data=yearly_referral_leads, marker='o')
plt.title('Persentase Pertumbuhan Jumlah Leads Referral per Tahun')
plt.xlabel('Tahun')
plt.ylabel('Pertumbuhan Persentase (%)')
plt.xticks(yearly_referral_leads['year'].unique()) #
plt.tight_layout()

# tambahkan keterangan persentase
ax = plt.gca()
for i, row in yearly_referral_leads.iterrows():
    # Only label if there is a growth percentage (i.e., not the first year's 0)
    if row['growth_percentage'] != 0:
        ax.annotate(f'{row['growth_percentage']:.1f}%',
                    (row['year'], row['growth_percentage']),
                    ha='center', va='bottom',
                    xytext=(0, 5),
                    textcoords='offset points',
                    fontsize=10,
                    color='black')
plt.show()

"""- penurunan yang signifikan dan konsisten dari jumlah leads yang bersumber dari referral, tren negatif ini kan menghambat akuisisi klien baru dan pertumbuhan bisnis

### Tren Tahunan dna pertumbuhan Leads closing per Year
"""

# Filter 'Referral' leads dengan 'Closing' status
closing_referral_leads = df_categorized[(df_categorized['type_of_leads_category'] == 'Referral') &
                                        (df_categorized['status_category'] == 'Closing')].copy()

# ekstrak year
closing_referral_leads['year'] = pd.to_datetime(closing_referral_leads['dated']).dt.year

# Group berdasrakan year
yearly_closing_referral_leads = closing_referral_leads.groupby('year').size().reset_index(name='count_closing_referral')

# hitung persentase pertumbuhan
yearly_closing_referral_leads['growth_percentage'] = yearly_closing_referral_leads['count_closing_referral'].pct_change() * 100
yearly_closing_referral_leads['growth_percentage'] = yearly_closing_referral_leads['growth_percentage'].fillna(0)

print("Yearly 'Closing' Referral Leads and Growth:")
display(yearly_closing_referral_leads)

# Visualisasi
plt.figure(figsize=(12, 6))
sns.lineplot(x='year', y='count_closing_referral', data=yearly_closing_referral_leads, marker='o')
plt.title('Jumlah Leads Referral dengan Status Closing per Tahun')
plt.xlabel('Tahun')
plt.ylabel('Jumlah Leads Closing')
plt.xticks(yearly_closing_referral_leads['year'].unique())
plt.grid(True)

# tambahkan keterangan angka
for index, row in yearly_closing_referral_leads.iterrows():
    plt.text(row['year'], row['count_closing_referral'] + 0.1,
             str(row['count_closing_referral']),
             color='black', ha='center', va='bottom', fontsize=10)

plt.tight_layout()
plt.show()

# Visualisasi
plt.figure(figsize=(12, 6))
sns.lineplot(x='year', y='growth_percentage', data=yearly_closing_referral_leads, marker='o')
plt.title('Persentase Pertumbuhan Leads Referral dengan Status Closing per Tahun')
plt.xlabel('Tahun')
plt.ylabel('Pertumbuhan Persentase (%)')
plt.xticks(yearly_closing_referral_leads['year'].unique())
plt.grid(True)
plt.tight_layout()

# Tambahkan keterangan persentase
ax = plt.gca()
for i, row in yearly_closing_referral_leads.iterrows():
    if row['growth_percentage'] != 0:
        ax.annotate(f'{row['growth_percentage']:.1f}%',
                    (row['year'], row['growth_percentage']),
                    ha='center', va='bottom',
                    xytext=(0, 5),
                    textcoords='offset points',
                    fontsize=10,
                    color='black')
plt.show()

"""## Tren Tahunan dan Pertumbuhan Revenue yang bersumber dari Referral"""

# hitung jumlah revenue dari referral berdasarkan tahun
yearly_value_fixed_closing_referral = closing_referral_leads.groupby('year')['value_fixed'].sum().reset_index(name='total_value_fixed_referral')

# hitung persentase pertumbuhan
yearly_value_fixed_closing_referral['growth_percentage'] = yearly_value_fixed_closing_referral['total_value_fixed_referral'].pct_change() * 100
yearly_value_fixed_closing_referral['growth_percentage'] = yearly_value_fixed_closing_referral['growth_percentage'].fillna(0) # Fill NaN for the first year with 0

print("Yearly Total 'Value_fixed' for Closing Referral Leads with Growth:")
display(yearly_value_fixed_closing_referral)

plt.figure(figsize=(12, 6))
sns.lineplot(x='year', y='total_value_fixed_referral', data=yearly_value_fixed_closing_referral, marker='o')
plt.title('Total Revenue dari Leads Referral per Tahun')
plt.xlabel('Tahun')
plt.ylabel('Total Value Fixed (IDR)')
plt.xticks(yearly_value_fixed_closing_referral['year'].unique())
plt.grid(True)
plt.ticklabel_format(style='plain', axis='y')

# tambahkan keterangan angka
for index, row in yearly_value_fixed_closing_referral.iterrows():
    plt.text(row['year'], row['total_value_fixed_referral'] + (yearly_value_fixed_closing_referral['total_value_fixed_referral'].max() * 0.02), # Adjust position dynamically
             f'Rp{row['total_value_fixed_referral']:,}',
             color='black', ha='center', va='bottom', fontsize=10, rotation=45)

plt.tight_layout()
plt.show()

plt.figure(figsize=(10, 6))
sns.lineplot(x='year', y='growth_percentage', data=yearly_value_fixed_closing_referral, marker='o')
plt.title('Persentase Pertumbuhan Revenue dari Leads Referral per Tahun')
plt.xlabel('Tahun')
plt.ylabel('Growth Percentage (%)')
plt.xticks(yearly_value_fixed_closing_referral['year'].unique())
plt.grid(True)
plt.tight_layout()

# tambahkan keterangan persentase
ax = plt.gca()
for i, row in yearly_value_fixed_closing_referral.iterrows():
    if row['growth_percentage'] != 0:
        ax.annotate(f'{row['growth_percentage']:.1f}%',
                    (row['year'], row['growth_percentage']),
                    ha='center', va='bottom',
                    xytext=(0, 5),
                    textcoords='offset points',
                    fontsize=10,
                    color='black')
plt.show()

"""- Revnue dari referral menunjukan trend negatif  yang konsisten, penurunan tertinggi terjadi pada tahun 2024 (51%) dan turun kembali pada tahun 2025(12%)

## Referral berdasarkan Status Category dan Service Category

### Perbandingan Leads 'Referral': Status 'Closing' vs. 'Status Lainnya'
"""

# Filter 'Referral' leads
referral_leads = df_categorized[df_categorized['type_of_leads_category'] == 'Referral'].copy()

# Buat kolom untuk status category closing dan lainnya
referral_leads['status_group'] = referral_leads['status_category'].apply(lambda x: 'Closing' if x == 'Closing' else 'Other Status')

# Hitung distribusi referral berdasarkan status
status_group_distribution_ref = referral_leads['status_group'].value_counts()

print("Distribution of 'Closing' vs. 'Other Status' for Referral Leads:")
display(status_group_distribution_ref)

# Hitung persentase
status_group_percentages_ref = (status_group_distribution_ref / status_group_distribution_ref.sum()) * 100
display(status_group_percentages_ref)

# Visualisasi
plt.figure(figsize=(10, 6))
ax = sns.barplot(x=status_group_percentages_ref.index, y=status_group_percentages_ref.values, palette='coolwarm', hue=status_group_percentages_ref.index, legend=False)
plt.title('Persentase Distribusi Closing vs Lainnya berdasarakan Referral Leads')
plt.xlabel('Status Group')
plt.ylabel('Percentage of Leads')
plt.xticks(rotation=0)
plt.tight_layout()

# Menambahkan keterangan persentase
for p in ax.patches:
    ax.annotate(f'{p.get_height():.1f}%',
                (p.get_x() + p.get_width() / 2., p.get_height()),
                ha='center', va='center',
                xytext=(0, 9),
                textcoords='offset points',
                fontsize=10,
                color='black')
plt.show()

"""closing rendah, konversi belum optimal, breakdown status category untuk melihat permasalahan dimana"""

# Filter  DataFrame untuk  'Referral' leads
referral_df= df_categorized[df_categorized['type_of_leads_category'] == 'Referral']

# hitung distribusi untuk Referral
status_distribution_r = referral_df['status_category'].value_counts()

print("Distribution of 'status_category' for 'Referral' leads:")
print(status_distribution_r)

# Hitung Persentase
status_percentages = status_distribution_r/ status_distribution_r.sum() * 100

# Visualisasi
plt.figure(figsize=(10, 6))
ax = sns.barplot(x=status_percentages.index, y=status_percentages.values, palette='viridis', hue=status_percentages.index, legend=False)
plt.title('Persentase  Distribusi Status Category berdasarkan Referral Leads')
plt.xlabel('Status Category')
plt.ylabel('Percentage of Leads')
plt.xticks(rotation=45, ha='right')
plt.tight_layout()

# Tambahkan keterangan Persentase
for p in ax.patches:
    ax.annotate(f'{p.get_height():.1f}%',
                (p.get_x() + p.get_width() / 2., p.get_height()),
                ha='center', va='center',
                xytext=(0, 9),
                textcoords='offset points')
plt.show()

"""- kirim proposal merupakan category yang mayoritas dengan (53,7%), artinya waktu setelah kirimm proposal adalah area kritis yang perlu dioptimalkan, merupakan potensi konversi yang belum optimal.
- closing cukup bagus dengan (30.9), artinya referral cukup berkualitas (menarik untuk tahu detail referralnya agar lebih bisa melihat kualitas referral)
- proses yang banya terhenti di proses awal (konsultasi, diskusi) menunjukan bahwa tahapan awal juga peru diperhatikan agar tidak ada yang terhenti

**Rekomendasi :**
- optimalisasi pasca-proposal  : follow-up proaktif, identifikasi Hambatan Konversi, Perkuat value proposition dalam proposal, berikan insentif terbatas waktu atau nilai tambah khsus untuk leads referral yang segera deal(closing)
- identifikasi sumber referral terbaik : untuk mengethui sumber yang menghasilkan klien dengan tigkat konversi tinggi, sehingga kita dapat melakukan pengembangan hubungan dengan referral yang paling efektif

### Distribusi Status Category Closing vs Lainnya Leads Referral berdasarkan Service Category
"""

service_closing_other_distribution_ref = referral_leads.groupby(['service_category_big', 'status_group']).size().unstack(fill_value=0)
service_closing_other_percentage_ref = service_closing_other_distribution_ref.apply(lambda x: x / x.sum() * 100, axis=1)

print("Distribution of 'Closing' vs. 'Other Status' for Referral Leads by Service Category:")
display(service_closing_other_distribution_ref)

print("\nPercentage Distribution:")
display(service_closing_other_percentage_ref)

# Visualisasi
plt.figure(figsize=(14, 8))
ax = service_closing_other_percentage_ref.plot(kind='bar', stacked=True, figsize=(14, 8), cmap='coolwarm')
plt.title('Persentase Distribusi Closing VS Lainnya berdasarkan Service Category untuk Referral Leads')
plt.xlabel('Service Category Big')
plt.ylabel('Percentage of Leads')
plt.xticks(rotation=45, ha='right')
plt.legend(title='Status Group', bbox_to_anchor=(1.05, 1), loc='upper left')

# Menambahkan keterangan persentase
for container in ax.containers:
    labels = [f'{w:.1f}%' if (w := v.get_height()) > 0 else '' for v in container]
    ax.bar_label(container, labels=labels, label_type='center', fontsize=9, color='white', fontweight='bold')

plt.tight_layout()
plt.show()

"""-  Kategori dengan Konversi Referral Terendah: 'Retainer' memiliki tingkat konversi closing yang sangat rendah (9.5%), dengan sebagian besar leads (90.5%) berakhir di 'Other Status'. Ini menunjukkan masalah fundamental dalam mengubah leads referral untuk layanan ini. 'Corporate' dan 'Imigrasi & TKA' juga menunjukkan tingkat closing yang rendah (masing-masing 18.2% dan 20.6%).
- Potensi Perbaikan di Banyak Kategori: Untuk sebagian besar kategori layanan, persentase leads referral yang berakhir di 'Other Status' lebih tinggi daripada yang 'Closing'. Contohnya, 'Litigasi' (33.3% Closing vs. 66.7% Other Status), 'Lainnya/Misc' (41.2% Closing vs. 58.8% Other Status), dan 'Family Law' (44.2% Closing vs. 55.8% Other Status).
- Fokus pada 'Other Status': Tingginya proporsi leads di 'Other Status' di hampir semua kategori mengindikasikan bahwa ada banyak leads referral yang masuk tetapi tidak berhasil dikonversi. Ini menunjukkan perlunya analisis lebih lanjut terhadap pipeline konversi untuk memahami mengapa leads ini tidak mencapai status closing.

### Detail Distribusi Status Category Leads Referral Berdasarkan Service Category
"""

service_status_distribution_referral = referral_leads.groupby(['service_category_big', 'status_category']).size().unstack(fill_value=0)

# Calculate percentages for visualization
service_status_percentage_referral = service_status_distribution_referral.apply(lambda x: x / x.sum() * 100, axis=1)

print("Detailed Distribution of Status Category for Referral Leads by Service Category:")
display(service_status_distribution_referral)

print("\nPercentage Distribution:")
display(service_status_percentage_referral)

plt.figure(figsize=(15, 9))
ax = service_status_percentage_referral.plot(kind='bar', stacked=True, figsize=(15, 9), cmap='viridis')
plt.title('Detail Persentase Distribusi Status Category  Referral Leads berdasarkan Service Category')
plt.xlabel('Service Category Big')
plt.ylabel('Percentage of Leads')
plt.xticks(rotation=45, ha='right')
plt.legend(title='Status Category', bbox_to_anchor=(1.05, 1), loc='upper left')

# Add percentage labels on the bars
for container in ax.containers:
    labels = [f'{w:.1f}%' if (w := v.get_height()) > 0 else '' for v in container]
    ax.bar_label(container, labels=labels, label_type='center', fontsize=8, color='white', fontweight='bold')

plt.tight_layout()
plt.show()

"""- Peluang Peningkatan Konversi:'Kirim Proposal' dan status lainnya ('Diskusi', 'Konsultasi', dll.) menunjukkan adanya potensi besar untuk meningkatkan tingkat konversi melalui strategi follow-up yang lebih proaktif, peninjauan proposal, atau penanganan keberatan yang lebih baik.

### Distribusi Revenue Referral Leads berdasarkan Service Category
"""

# Group by 'service_category_big' for closing referral leads
closing_referral_by_service = closing_referral_leads.groupby('service_category_big').agg(
    count_closing=('leads name', 'count'),
    total_value_fixed=('value_fixed', 'sum')
).reset_index()

closing_referral_by_service = closing_referral_by_service.sort_values(by='total_value_fixed', ascending=False)

print("Analisis Referral Leads dengan Status 'Closing' Berdasarkan Kategori Layanan:")
display(closing_referral_by_service)

# Group Revenue by Service Catgory
closing_referral_by_service = closing_referral_leads.groupby('service_category_big').agg(
    count_closing=('leads name', 'count'),
    total_value_fixed=('value_fixed', 'sum')
).reset_index()

closing_referral_by_service = closing_referral_by_service.sort_values(by='total_value_fixed', ascending=False)

plt.figure(figsize=(12, 7))
sns.barplot(x='service_category_big', y='total_value_fixed', data=closing_referral_by_service, palette='plasma', hue='service_category_big', legend=False)
plt.title('Total Revenue dari Leads Referral Closing per Kategori Layanan')
plt.xlabel('Kategori Layanan')
plt.ylabel('Total Value Fixed (IDR)')
plt.xticks(rotation=45, ha='right')
plt.ticklabel_format(style='plain', axis='y') # Prevent scientific notation on y-axis
plt.grid(axis='y', linestyle='--', alpha=0.7)

# Add numerical labels on top of each bar
for p in plt.gca().patches:
    plt.gca().annotate(f'Rp{p.get_height():,.0f}',
                (p.get_x() + p.get_width() / 2., p.get_height()),
                ha='center', va='center',
                xytext=(0, 9),
                textcoords='offset points',
                fontsize=10,
                color='black')
plt.tight_layout()
plt.show()

"""- Family Law, mendominasi jumlah closing dan revenue, closing 19 leads dan kontribusi revenue sebesar  Rp. 413,5 juta, menunjukan Family law berhasil mengkonversi leads bernilai tinggi
- Lainnya/ Misc kontibutor yang kuat dengan closing 14, tingkat konversi (41,2%) dan revenue Rp. 219,5 juta
- Imigrasi & TKA, service  category potensial dengan revenue cukup besar Rp. 190 juta, dengan leads yang relatif rendah 7.  menunjukan bahwa leads referral category ini bernilai tinggi, tantangannya konversi masih sangat rendah 20% closing

## Analsis Referrer
"""

specific_referral_df = df_categorized[
    (df_categorized['type_of_leads_clean'].str.startswith('Referral')) |
    (df_categorized['type_of_leads_clean'] == 'Unknown Referrer')
].copy()

specific_referral_df['dated'] = pd.to_datetime(specific_referral_df['dated'])
specific_referral_df['year'] = specific_referral_df['dated'].dt.year

print("Filtered DataFrame for Specific Referral Leads:")
display(specific_referral_df.head())

# Group unik referrer
yearly_unique_referrer_types = specific_referral_df.groupby('year')['type_of_leads_clean'].nunique().reset_index(name='distinct_referral_types_count')

# hitung pertumbuhan
yearly_unique_referrer_types['growth_percentage'] = yearly_unique_referrer_types['distinct_referral_types_count'].pct_change() * 100
yearly_unique_referrer_types['growth_percentage'] = yearly_unique_referrer_types['growth_percentage'].fillna(0)

print("Number of Distinct Referrer Types per Year and their Growth:")
display(yearly_unique_referrer_types)

# Group
yearly_unique_referrar_types = specific_referral_df.groupby('year')['type_of_leads_clean'].nunique().reset_index(name='distinct_referral_types_count')

# visulaisasi
plt.figure(figsize=(12, 6))
sns.lineplot(x='year', y='distinct_referral_types_count', data=yearly_unique_referrer_types, marker='o')
plt.title('Jumlah Referrer Unik per Tahun')
plt.xlabel('Tahun')
plt.ylabel('Jumlah Jenis Referral Unik')
plt.xticks(yearly_unique_referrer_types['year'].unique())
plt.grid(True)

# tambahkan keterangan angka
for index, row in yearly_unique_referrer_types.iterrows():
    plt.text(row['year'], row['distinct_referral_types_count'] + 0.1,
             str(row['distinct_referral_types_count']),
             color='black', ha='center', va='bottom', fontsize=10)

plt.tight_layout()
plt.show()

"""- penurunan konsisten jumlah unik referral
- indikasi hilangnya sumber referral
- potensi kehilangan jangkauan jaringan segment pasar berbeda atau jenis klien yang unik
"""

closing_specific_referrer_df = specific_referral_df[
    specific_referral_df['status_category'] == 'Closing'
].copy()

# Group berdasarkan tahun referrer unik  yang berhasil closing
yearly_unique_closing_referrer_types = closing_specific_referrer_df.groupby('year')['type_of_leads_clean'].nunique().reset_index(name='distinct_closing_referral_types_count')

# kalkulasi persentase pertumbuhan referral  yang closing
yearly_unique_closing_referrer_types['growth_percentage'] = yearly_unique_closing_referrer_types['distinct_closing_referral_types_count'].pct_change() * 100
yearly_unique_closing_referrer_types['growth_percentage'] = yearly_unique_closing_referrer_types['growth_percentage'].fillna(0)
print("Number of Distinct Closing Referrer Types per Year and their Growth:")
display(yearly_unique_closing_referrer_types)

plt.figure(figsize=(12, 6))
sns.lineplot(x='year', y='distinct_closing_referral_types_count', data=yearly_unique_closing_referrer_types, marker='o')
plt.title('Jumlah Referal Unik yang Closing per Tahun')
plt.xlabel('Tahun')
plt.ylabel('Jumlah Jenis Referrer Unik Closing')
plt.xticks(yearly_unique_closing_referrer_types['year'].unique())
plt.grid(True)

# tambahkan keterangan angka
for index, row in yearly_unique_closing_referrer_types.iterrows():
    plt.text(row['year'], row['distinct_closing_referral_types_count'] + 0.1,
             str(row['distinct_closing_referral_types_count']),
             color='black', ha='center', va='bottom', fontsize=10)

plt.tight_layout()
plt.show()

plt.figure(figsize=(12, 6))
sns.lineplot(x='year', y='growth_percentage', data=yearly_unique_referrer_types, marker='o')
plt.title('Persentase Pertumbuhan Jumlah Jenis Referrer Unik per Tahun')
plt.xlabel('Tahun')
plt.ylabel('Pertumbuhan Persentase (%)')
plt.xticks(yearly_unique_referrer_types['year'].unique()) # Ensure all years are shown on x-axis
plt.grid(True)
plt.tight_layout()

# Add percentage labels on top of each point
ax = plt.gca()
for i, row in yearly_unique_referrer_types.iterrows():
    if row['growth_percentage'] != 0:
        ax.annotate(f'{row['growth_percentage']:.1f}%',
                    (row['year'], row['growth_percentage']),
                    ha='center', va='bottom',
                    xytext=(0, 5),
                    textcoords='offset points',
                    fontsize=10,
                    color='black')
plt.show()

"""- penurunan drastis dan konsisten dari unik referrer yang closing
- penurunan efektivivitas referrer atau referral yang masuk tidak sesuai dengna kebutuhan firma
- ketergantungan pada satu atau dua sumber referral yang berhasil closing meningkatkan resiko/ kerentanan

## Analisis Performa Referrer
"""

# Group referrer
specific_referrer_performance = closing_referral_leads.groupby('type_of_leads_clean').agg(
    count_closing=('leads name', 'count'),
    total_value_fixed=('value_fixed', 'sum')
).reset_index()

# urutkan berdasarkan closing
top_referrers_by_count = specific_referrer_performance.sort_values(by='count_closing', ascending=False)
print("Top Referrers by Number of Closing Leads:")
display(top_referrers_by_count)

# urutkan berdasarkan revenue
top_referrers_by_revenue = specific_referrer_performance.sort_values(by='total_value_fixed', ascending=False)
print("\nTop Referrers by Total Value Fixed (Revenue):")
display(top_referrers_by_revenue)

fig, axes = plt.subplots(1, 2, figsize=(20, 8))

# Top 10 Referrer yang berhasil closing
sns.barplot(ax=axes[0], x='type_of_leads_clean', y='count_closing', data=top_referrers_by_count.head(10), palette='viridis', hue='type_of_leads_clean', legend=False)
axes[0].set_title('Top 10 Referrers berdasarkan jumlah Closing Leads')
axes[0].set_xlabel('Jenis Referral')
axes[0].set_ylabel('Jumlah Leads Closing')
axes[0].tick_params(axis='x', rotation=45)
axes[0].grid(axis='y', linestyle='--', alpha=0.7)
# tambahkan angka
for container in axes[0].containers:
    axes[0].bar_label(container, fmt='%d', label_type='edge', padding=3)

# Top 10 Referrer dengan Total Revenue
sns.barplot(ax=axes[1], x='type_of_leads_clean', y='total_value_fixed', data=top_referrers_by_revenue.head(10), palette='plasma', hue='type_of_leads_clean', legend=False)
axes[1].set_title('Top 10 Referrers berdasarkan Total Revenue')
axes[1].set_xlabel('Jenis Referral')
axes[1].set_ylabel('Total Value Fixed (IDR)')
axes[1].tick_params(axis='x', rotation=45)
axes[1].ticklabel_format(style='plain', axis='y')
axes[1].grid(axis='y', linestyle='--', alpha=0.7)
# tambahkan angka
for container in axes[1].containers:
    axes[1].bar_label(container, fmt='%d', label_type='edge', padding=3, rotation=45)

plt.tight_layout()
plt.show()

"""- dominasi **Referral RT** baik dari jumlah closing (29) maupun revenue (Rp. 678 juta) menunjukan referral RT yang sangat berkualitas tinggi dan penting bagi pendapatan perusahaan
- **Referral BPL, Unknown Referrer dan Referral Stefa**, termasuk kedalam referrer penting lainnya karena konsisten berada di TOP 5 baik dari closing maupun Revenue
- Indikasi ada leads dengan nilai tinggi, Referrer seperti ** Referral AH, dan Referral Anggelina** meskipun hanya 1 closing menunjukan revenue yang baik masing-masing 35 juta
- **Unknown Referrer**: memiliki peforma yang baik namun tidak teridentifikasi sehingga ada kehilangan peluang untuk membangun hubungan strategis dan optimalisasi sumber tersebut

### Referrer closing berdasarkan Tahun
"""

# Groupberdasarkan  year dan kolom 'type_of_leads_clean' dan hitung jumlah leads closing
yearly_closing_referral_by_type = closing_referral_leads.groupby(['year', 'type_of_leads_clean']).size().unstack(fill_value=0)

print("Jumlah Leads Referral dengan Status 'Closing' per Jenis Referral dan per Tahun:")
display(yearly_closing_referral_by_type)

"""- Referral RT konsisten berkontibusi dari tahun 2022-2025 dengan kontribusi tertinggi paa tahun 2023 dengan 3 closing
- dari tahun 2024 referral banyak yang peforma menurun

### Perbandingan Kinerja Berbagai Kategori Leads (Repeat Order, Referral, New Prospect, New Organic)
"""

lead_category_performance = df_categorized.groupby('type_of_leads_category').agg(
    total_leads=('leads name', 'count'),
    closing_leads=('status_category', lambda x: (x == 'Closing').sum()),
    total_revenue=('value_fixed', lambda x: x[df_categorized['status_category'] == 'Closing'].sum())
).reset_index()

# hitung konversi rate
lead_category_performance['conversion_rate'] = (lead_category_performance['closing_leads'] / lead_category_performance['total_leads']) * 100

# kalkulasi  rata-tata revenue
lead_category_performance['avg_revenue_per_closing_lead'] = lead_category_performance.apply(
    lambda row: row['total_revenue'] / row['closing_leads'] if row['closing_leads'] > 0 else 0,
    axis=1
)

print("Ringkasan Kinerja Berbagai Kategori Leads:")
display(lead_category_performance.sort_values(by='total_revenue', ascending=False))

# hitung non-closing leads
lead_category_performance['non_closing_leads'] = lead_category_performance['total_leads'] - lead_category_performance['closing_leads']

# urutkan servcice category berdasarkan non-closing leads
sorted_performance = lead_category_performance.sort_values(by='total_leads', ascending=False)

df_stacked = sorted_performance.melt(
    id_vars=['type_of_leads_category'],
    value_vars=['non_closing_leads', 'closing_leads'],
    var_name='lead_status',
    value_name='count'
)
display(df_stacked.head())

"""#### Visualisasi Perbandingan Jumlah Leads dan Leads Closing per Kategori"""

# Get unique categories for plotting
categories = sorted_performance['type_of_leads_category'].tolist()
x = range(len(categories))

# Plotting using plt.bar for explicit stacking
bottom_values = [0] * len(categories)

# Plot non-closing leads (bottom layer)
for i, category in enumerate(categories):
    non_closing_count = df_stacked[(df_stacked['type_of_leads_category'] == category) & (df_stacked['lead_status'] == 'non_closing_leads')]['count'].iloc[0]
    plt.bar(i, non_closing_count, bottom=bottom_values[i], color='darkred', width=0.8, label='Non-Closing Leads' if i == 0 else "")
    plt.text(i, bottom_values[i] + non_closing_count / 2, str(int(non_closing_count)),
             ha='center', va='center', color='white', fontsize=10, fontweight='bold')
    bottom_values[i] += non_closing_count

# Plot closing leads (top layer)
for i, category in enumerate(categories):
    closing_count = df_stacked[(df_stacked['type_of_leads_category'] == category) & (df_stacked['lead_status'] == 'closing_leads')]['count'].iloc[0]
    plt.bar(i, closing_count, bottom=bottom_values[i], color='darkblue', width=0.8, label='Closing Leads' if i == 0 else "")
    plt.text(i, bottom_values[i] + closing_count / 2, str(int(closing_count)),
             ha='center', va='center', color='white', fontsize=10, fontweight='bold')
    bottom_values[i] += closing_count


plt.title('Perbandingan Closing vs. Non-Closing Leads per Kategori', fontsize=16)
plt.xlabel('Kategori Leads', fontsize=12)
plt.ylabel('Jumlah Leads', fontsize=12)
plt.xticks(x, categories, rotation=45, ha='right')
plt.legend(title='Lead Status', loc='upper right', fontsize=10)
plt.grid(axis='y', linestyle='--', alpha=0.7)

plt.tight_layout()
plt.show()

"""#### Visualisasi Tingkat Konversi per Kategori Leads"""

plt.figure(figsize=(10, 6))
ax = sns.barplot(x='type_of_leads_category', y='conversion_rate', data=lead_category_performance.sort_values(by='conversion_rate', ascending=False), palette='magma', hue='type_of_leads_category', legend=False)
plt.title('Tingkat Konversi (Closing Leads %) per Kategori Leads')
plt.xlabel('Kategori Leads')
plt.ylabel('Tingkat Konversi (%)')
plt.xticks(rotation=45, ha='right')
plt.grid(axis='y', linestyle='--', alpha=0.7)

# Add percentage labels on top of each bar
for p in ax.patches:
    ax.annotate(f'{p.get_height():.1f}%',
                (p.get_x() + p.get_width() / 2., p.get_height()),
                ha='center', va='center',
                xytext=(0, 9),
                textcoords='offset points',
                fontsize=10,
                color='black')
plt.tight_layout()
plt.show()

"""#### Visualisasi Total Revenue per Kategori Leads"""

plt.figure(figsize=(10, 6))
ax = sns.barplot(x='type_of_leads_category', y='total_revenue', data=lead_category_performance.sort_values(by='total_revenue', ascending=False), palette='viridis', hue='type_of_leads_category', legend=False)
plt.title('Total Revenue dari Leads Closing per Kategori Leads')
plt.xlabel('Kategori Leads')
plt.ylabel('Total Revenue (IDR)')
plt.xticks(rotation=45, ha='right')
plt.ticklabel_format(style='plain', axis='y')
plt.grid(axis='y', linestyle='--', alpha=0.7)

# Add revenue labels on top of each bar
for p in ax.patches:
    ax.annotate(f'Rp{p.get_height():,.0f}',
                (p.get_x() + p.get_width() / 2., p.get_height()),
                ha='center', va='center',
                xytext=(0, 9),
                textcoords='offset points',
                fontsize=10,
                color='black')
plt.tight_layout()
plt.show()

"""#### Visualisasi Rata-rata Revenue per Closing Lead per Kategori Leads"""

plt.figure(figsize=(10, 6))
ax = sns.barplot(x='type_of_leads_category', y='avg_revenue_per_closing_lead', data=lead_category_performance.sort_values(by='avg_revenue_per_closing_lead', ascending=False), palette='crest', hue='type_of_leads_category', legend=False)
plt.title('Rata-rata Revenue per Closing Lead per Kategori Leads')
plt.xlabel('Kategori Leads')
plt.ylabel('Rata-rata Revenue per Closing Lead (IDR)')
plt.xticks(rotation=45, ha='right')
plt.ticklabel_format(style='plain', axis='y')
plt.grid(axis='y', linestyle='--', alpha=0.7)

# Add average revenue labels on top of each bar
for p in ax.patches:
    ax.annotate(f'Rp{p.get_height():,.0f}',
                (p.get_x() + p.get_width() / 2., p.get_height()),
                ha='center', va='center',
                xytext=(0, 9),
                textcoords='offset points',
                fontsize=10,
                color='black')
plt.tight_layout()
plt.show()

"""#**Forecast**

##Forecast Repeat Order

### Forecast Revenue for Repeat Order using Prophet
"""

# Ensure prophet is installed
!pip install prophet

# Import Prophet
from prophet import Prophet

# Filter for 'Repeat Order' leads with 'Closing' status
repeat_order_closing_df = df_categorized[(df_categorized['type_of_leads_category'] == 'Repeat Order') &
                                          (df_categorized['status_category'] == 'Closing')].copy()

# Ensure 'dated' is datetime type
repeat_order_closing_df['dated'] = pd.to_datetime(repeat_order_closing_df['dated'])

# Group by date and sum 'value_fixed' to get daily revenue
daily_repeat_order_revenue = repeat_order_closing_df.groupby('dated')['value_fixed'].sum().reset_index()

# Prepare data for Prophet: 'ds' for date, 'y' for value
daily_repeat_order_revenue = daily_repeat_order_revenue.rename(columns={'dated': 'date', 'value_fixed': 'revenue'})

# Display the prepared data
print("Prepared data for Prophet:")
display(daily_repeat_order_revenue.head())

# Initialize Prophet model
m = Prophet(
    yearly_seasonality=True,
    weekly_seasonality=True,
    daily_seasonality=False  # Assuming daily data might not have strong daily patterns, or too sparse
)

# Fit the model to the data
m.fit(daily_repeat_order_revenue)

print("Prophet model fitted successfully.")

import pandas as pd

# Create a DataFrame for future predictions
# The last training date is 2026-01-12. To forecast until 2026-12-31 (inclusive), we need 353 daily periods (from 2026-01-13 to 2026-12-31).
future = m.make_future_dataframe(periods=353, freq='D')

# Make predictions, storing the raw forecast with Prophet's default column names
raw_forecast = m.predict(future)

# Create a separate DataFrame for display with renamed columns for better readability
forecast_display = raw_forecast.rename(columns={'ds': 'date', 'yhat': 'revenue', 'yhat_lower': 'revenue_lower', 'yhat_upper': 'revenue_upper'})

# Filter the forecast_display to show only April to December 2026
forecast_filtered = forecast_display[(forecast_display['date'] >= '2026-04-01') & (forecast_display['date'] <= '2026-12-31')]

# Display the filtered forecast (head and tail to show the range)
print("Forecast for April to December 2026 (first 5 rows):")
display(forecast_filtered[['date', 'revenue', 'revenue_lower', 'revenue_upper']].head())
print("Forecast for April to December 2026 (last 5 rows):")
display(forecast_filtered[['date', 'revenue', 'revenue_lower', 'revenue_upper']].tail())

# Plot the forecast using the raw_forecast DataFrame which retains Prophet's default column names
fig1 = m.plot(raw_forecast)
plt.title('Repeat Order Revenue Forecast')
plt.xlabel('Date')
plt.ylabel('Revenue (IDR)')
plt.ticklabel_format(style='plain', axis='y')
plt.show()

# Plot the components of the forecast (trend, yearly seasonality, weekly seasonality)
fig2 = m.plot_components(raw_forecast)
plt.show()

"""**Insight :**

Dari Forecast Revenue Repeat Order (April - Desember 2026):

**1. Tren Penurunan yang Berkelanjutan**: Grafik utama (fig1) kemungkinan besar akan menunjukkan tren penurunan yang terus berlanjut pada revenue dari Repeat Order di tahun 2026. Ini sejalan dengan analisis historis yang menunjukkan penurunan tajam sejak tahun 2024.

**Implikasi Bisnis**: Firma menghadapi tantangan serius dalam mempertahankan nilai transaksi dari klien repeat. Tanpa intervensi yang signifikan, kontribusi pendapatan dari segmen ini akan terus menyusut, berpotensi mempengaruhi stabilitas keuangan jangka panjang.

**2. Pola Musiman Tahunan (Yearly Seasonality)**: Grafik komponen (fig2) akan menunjukkan bagaimana revenue berfluktuasi sepanjang tahun. Ada kemungkinan adanya puncak dan lembah pada bulan-bulan tertentu, meskipun tren keseluruhan menurun.

**Implikasi Bisnis**: Memahami bulan-bulan puncak atau lesu dapat membantu dalam perencanaan strategi pemasaran dan alokasi sumber daya. Misalnya, jika ada penurunan di tengah tahun, ini bisa menjadi waktu yang tepat untuk kampanye retensi khusus.

**3. Pola Musiman Mingguan (Weekly Seasonality)**: Grafik komponen juga akan memperlihatkan hari-hari dalam seminggu dengan revenue yang lebih tinggi atau lebih rendah. Ini umumnya stabil dan mungkin menunjukkan hari kerja lebih aktif daripada akhir pekan, atau pola lain yang spesifik.

Implikasi Bisnis: Ini dapat menginformasikan kapan waktu terbaik untuk melakukan follow-up, mengirim proposal, atau meluncurkan promosi untuk klien repeat, sehingga dapat memaksimalkan peluang closing.

**4. Ketidakpastian Forecast (Prediction Interval)**: Area abu-abu pada grafik forecast utama menunjukkan rentang kepercayaan. Jika rentang ini lebar, itu menandakan bahwa model memiliki ketidakpastian tinggi, yang mungkin disebabkan oleh fluktuasi historis yang besar atau data yang tidak konsisten.

**Implikasi Bisnis**: Ketidakpastian yang tinggi berarti manajemen harus berhati-hati dalam mengandalkan angka tunggal dari forecast dan mempertimbangkan skenario terburuk.

**Rekomendasi Bisnis:**

- Analisis Mendalam Kualitas Layanan: Lakukan survei kepuasan klien repeat secara proaktif untuk memahami faktor-faktor penyebab ketidakpuasan atau mengapa mereka tidak kembali. Bandingkan layanan yang diberikan dengan ekspektasi klien.

-  Program Loyalitas dan Value Proposition yang Diperbarui: Tawarkan insentif yang lebih menarik dan personal untuk klien repeat, seperti diskon eksklusif untuk layanan lanjutan, konsultasi gratis untuk kasus baru, akses prioritas ke seminar hukum, atau dedicated relationship manager. Pastikan value proposition ini dikomunikasikan dengan jelas.

- Optimasi Proses Closing Pasca-Proposal: Analisis sebelumnya menyoroti 'Kirim Proposal' sebagai bottleneck utama. Fokus pada peningkatan konversi di tahap ini:

- Follow-up Proaktif dan Personalisasi: Tingkatkan intensitas dan kualitas follow-up setelah pengiriman proposal. Pastikan komunikasi bersifat personal, menjawab semua pertanyaan, dan mengatasi keberatan klien secara efektif.
Penyempurnaan Proposal: Kaji ulang format, isi, dan value proposition dalam proposal agar lebih persuasif dan menyoroti keuntungan spesifik bagi klien repeat.

- Insentif Waktu Terbatas: Pertimbangkan untuk menawarkan insentif berbatas waktu untuk mendorong keputusan closing yang lebih cepat dari klien repeat yang sudah menerima proposal.

Pemanfaatan Pola Musiman: Meskipun tren menurun, pola musiman dapat dimanfaatkan untuk memitigasi dampak:

- Kampanye Bertarget: Rencanakan kampanye pemasaran atau penjangkauan (outreach) yang menargetkan klien repeat pada periode-periode ketika revenue cenderung lebih tinggi berdasarkan pola musiman tahunan dan mingguan.

- Alokasi Sumber Daya: Sesuaikan alokasi tim penjualan dan tim layanan klien untuk mengantisipasi periode sibuk atau lambat, memastikan mereka siap untuk memaksimalkan peluang closing.

- Diversifikasi Sumber Revenue: Meskipun Repeat Order penting, penurunan ini juga menekankan perlunya evaluasi strategi akuisisi klien baru (misalnya, New Prospect, Referral, New Organic) untuk memastikan firma tidak terlalu bergantung pada satu segmen yang rentan.

**Forecast ini menjadi sinyal merah bagi firma untuk segera melakukan evaluasi menyeluruh dan menerapkan strategi agresif untuk merevitalisasi segmen Repeat Order guna menghindari kerugian pendapatan yang lebih besar di masa mendatang**

## Forecast Revenue for Referral using Prophet
"""

# Ensure prophet is installed
!pip install prophet

# Import Prophet
from prophet import Prophet

# Filter for 'Referral' leads with 'Closing' status
referral_closing_prophet_df = df_categorized[(df_categorized['type_of_leads_category'] == 'Referral') &
                                          (df_categorized['status_category'] == 'Closing')].copy()

# Ensure 'dated' is datetime type
referral_closing_prophet_df['dated'] = pd.to_datetime(referral_closing_prophet_df['dated'])

# Group by date and sum 'value_fixed' to get daily revenue
daily_referral_revenue_prophet = referral_closing_prophet_df.groupby('dated')['value_fixed'].sum().reset_index()

# Prepare data for Prophet: 'ds' for date, 'y' for value
daily_referral_revenue_prophet = daily_referral_revenue_prophet.rename(columns={'dated': 'ds', 'value_fixed': 'y'})

# Display the prepared data
print("Prepared data for Prophet (first 5 rows):")
display(daily_referral_revenue_prophet.head())
print("Prepared data for Prophet (last 5 rows):")
display(daily_referral_revenue_prophet.tail())

# Initialize Prophet model
m_referral = Prophet(
    yearly_seasonality=True,
    weekly_seasonality=True,
    daily_seasonality=False  # Assuming daily data might not have strong daily patterns, or too sparse
)

# Fit the model to the data
m_referral.fit(daily_referral_revenue_prophet)

print("Prophet model for referral revenue fitted successfully.")

import pandas as pd

# Create a DataFrame for future predictions
# The last training date is from the dataset. To forecast until 2026-12-31 (inclusive),
# calculate periods needed from the last date in daily_referral_revenue_prophet to 2026-12-31.
last_date_in_data = daily_referral_revenue_prophet['ds'].max()
forecast_end_date = pd.to_datetime('2026-12-31')

# Calculate the number of periods needed to reach forecast_end_date
periods_to_forecast = (forecast_end_date - last_date_in_data).days

future_referral = m_referral.make_future_dataframe(periods=periods_to_forecast, freq='D')

# Make predictions, storing the raw forecast with Prophet's default column names
raw_forecast_referral = m_referral.predict(future_referral)

# Create a separate DataFrame for display with renamed columns for better readability
forecast_display_referral = raw_forecast_referral.rename(columns={'ds': 'date', 'yhat': 'revenue', 'yhat_lower': 'revenue_lower', 'yhat_upper': 'revenue_upper'})

# Filter the forecast_display_referral to show only April to December 2026
# Explicitly create a copy to avoid SettingWithCopyWarning
forecast_filtered_referral = forecast_display_referral[(forecast_display_referral['date'] >= '2026-04-01') & (forecast_display_referral['date'] <= '2026-12-31')].copy()

# Ensure revenue values are not negative
forecast_filtered_referral['revenue'] = forecast_filtered_referral['revenue'].apply(lambda x: max(0, x))
forecast_filtered_referral['revenue_lower'] = forecast_filtered_referral['revenue_lower'].apply(lambda x: max(0, x))
forecast_filtered_referral['revenue_upper'] = forecast_filtered_referral['revenue_upper'].apply(lambda x: max(0, x))

# Display the filtered forecast (head and tail to show the range)
print("Forecast for April to December 2026 (first 5 rows):")
display(forecast_filtered_referral[['date', 'revenue', 'revenue_lower', 'revenue_upper']].head())
print("Forecast for April to December 2026 (last 5 rows):")
display(forecast_filtered_referral[['date', 'revenue', 'revenue_lower', 'revenue_upper']].tail())

# Plot the forecast using the raw_forecast_referral DataFrame which retains Prophet's default column names
import matplotlib.pyplot as plt

fig_referral_1 = m_referral.plot(raw_forecast_referral)
plt.title('Referral Revenue Forecast (Prophet)')
plt.xlabel('Date')
plt.ylabel('Revenue (IDR)')
plt.ticklabel_format(style='plain', axis='y')
plt.show()

# Plot the components of the forecast (trend, yearly seasonality, weekly seasonality)
fig_referral_2 = m_referral.plot_components(raw_forecast_referral)
plt.show()

"""**Insight dari Forecast Revenue Referral (Prophet):**

1.  **Tren Penurunan yang Kuat**: Mirip dengan model ARIMA, forecast Prophet kemungkinan besar akan menunjukkan tren penurunan yang signifikan dan berkelanjutan pada revenue dari Referral hingga akhir tahun 2026. Ini memperkuat temuan bahwa segmen referral sedang menghadapi tantangan serius.

2.  **Pola Musiman Tahunan dan Mingguan**: Prophet secara eksplisit menguraikan komponen musiman. Akan terlihat pola bagaimana revenue berfluktuasi sepanjang tahun (yearly seasonality) dan dalam seminggu (weekly seasonality). Pola ini bisa jadi lebih jelas atau berbeda dari yang diasumsikan oleh ARIMA, memberikan pemahaman yang lebih granular.

3.  **Ketidakpastian yang Terlihat**: Rentang kepercayaan (area abu-abu pada plot forecast) akan memberikan gambaran tentang tingkat ketidakpastian dalam prediksi. Jika rentang ini lebar, itu menunjukkan model memiliki keraguan yang tinggi, mungkin karena volatilitas data historis atau kurangnya pola yang kuat untuk diekstrapolasi.

**Perbandingan dengan ARIMA (potensi perbedaan):**

*   **Penanganan Seasonality**: Prophet cenderung lebih baik dalam menangani pola musiman yang kompleks dan liburan, yang mungkin memberikan estimasi musiman yang lebih akurat dibandingkan ARIMA standar jika pola tersebut ada dalam data.
*   **Interpretasi Komponen**: Prophet memecah forecast menjadi tren, seasonality, dan hari libur (jika ditambahkan), yang membuatnya lebih mudah diinterpretasikan daripada parameter p, d, q ARIMA.
*   **Robustness**: Prophet dirancang untuk menjadi lebih robust terhadap missing values dan outliers.

**Rekomendasi Bisnis (menggabungkan insight dari Prophet dan ARIMA):**

1.  **Prioritaskan Strategi Revitalisasi Referral**: Tren penurunan dari kedua model mengindikasikan urgensi. Firma harus segera mengimplementasikan strategi komprehensif untuk mengidentifikasi dan mengatasi akar masalah penurunan referral.

2.  **Manfaatkan Insight Musiman untuk Kampanye Tertarget**: Gunakan pola musiman tahunan dan mingguan dari Prophet untuk menjadwalkan kampanye insentif referral, acara apresiasi referrer, atau komunikasi strategis. Misalnya, jika ada periode puncak referral, fokuskan upaya maksimal di sana.

3.  **Fokus pada Penguatan Hubungan dengan Referrer Kunci**: Lanjutkan untuk mengidentifikasi dan memberikan apresiasi serta insentif kepada referrer berkinerja tinggi (seperti 'Referral RT'). Jika ada pola musiman yang menguntungkan mereka, manfaatkan itu untuk memperkuat hubungan.

4.  **Tingkatkan Proses Konversi di Tahap 'Kirim Proposal'**: Baik Prophet maupun analisis sebelumnya menyoroti bottleneck ini. Investasikan dalam pelatihan tim penjualan, perbaikan materi proposal, dan sistem follow-up yang lebih efisien.

5.  **Perbaiki Kualitas Data dan Pertimbangkan Variabel Eksternal**: Jika ketidakpastian forecast (rentang kepercayaan) tinggi, ini bisa menjadi sinyal bahwa model memerlukan lebih banyak data atau variabel eksternal (misalnya, kondisi ekonomi, strategi kompetitor, perubahan kebijakan) untuk meningkatkan akurasi. Data tentang 'Unknown Referrer' harus diidentifikasi untuk memperbaiki model di masa depan.

**Kesimpulan**: Forecast Prophet untuk Referral Revenue secara konsisten menunjukkan tantangan serius yang dihadapi firma. Intervensi strategis yang cepat dan terencana sangat penting untuk mengubah tren negatif ini dan memastikan kelangsungan kontribusi referral terhadap pendapatan.

##Forecast Revenue by Referral ARIMA Model

### Data Preparation for ARIMA Model

To prepare the data for the ARIMA model, we'll follow these steps:

1.  **Filter Data**: Select only 'Referral' leads that have a 'Closing' status.
2.  **Convert Date Column**: Ensure the 'dated' column is in datetime format.
3.  **Aggregate Daily Revenue**: Group the data by date and sum the `value_fixed` column to get the total daily revenue.
4.  **Create Continuous Time Series**: Generate a complete date range from the earliest available date to the end of the forecast period. Then, reindex the daily revenue data to this complete range, filling any missing dates with zero revenue. This ensures a continuous time series, which is crucial for ARIMA models.
5.  **Rename Columns**: Rename the date column to 'ds' and the revenue column to 'y' to match common time series conventions.
"""

# Ensure statsmodels is installed
!pip install statsmodels

import pandas as pd

# Filter for 'Referral' leads with 'Closing' status
referral_closing_df = df_categorized[(df_categorized['type_of_leads_category'] == 'Referral') &
                                       (df_categorized['status_category'] == 'Closing')].copy()

# Ensure 'dated' is datetime type
referral_closing_df['dated'] = pd.to_datetime(referral_closing_df['dated'])

# Group by date and sum 'value_fixed' to get daily revenue
daily_referral_revenue = referral_closing_df.groupby('dated')['value_fixed'].sum().reset_index()

# Create a complete date range from the first date in our data to the end of 2026
min_date = daily_referral_revenue['dated'].min()
max_date_forecast = pd.to_datetime('2026-12-31')
full_date_range = pd.date_range(start=min_date, end=max_date_forecast, freq='D')

# Reindex the daily revenue data to the full date range, filling missing values (days with no sales) with 0
daily_referral_revenue_full = daily_referral_revenue.set_index('dated').reindex(full_date_range, fill_value=0).reset_index()
daily_referral_revenue_full = daily_referral_revenue_full.rename(columns={'index': 'ds', 'value_fixed': 'y'})

# Display the prepared data
print("Prepared data for ARIMA model (first 5 rows):")
display(daily_referral_revenue_full.head())
print("Prepared data for ARIMA model (last 5 rows):")
display(daily_referral_revenue_full.tail())

"""### ARIMA Model Overview and Parameter Selection

ARIMA (AutoRegressive Integrated Moving Average) is a popular statistical model for time series forecasting. It's defined by three components: `p`, `d`, and `q`.

*   **AR (p)**: AutoRegressive refers to the number of lagged (past) observations to include in the model. A 'p' value of 1 means that the forecast for the current time step is based on the previous time step's value.
*   **I (d)**: Integrated refers to the differencing order. This is the number of times the raw observations are differenced to make the time series stationary (i.e., remove trends or seasonality). A 'd' value of 1 means the first-order difference is used.
*   **MA (q)**: Moving Average refers to the number of lagged forecast errors to include in the model. A 'q' value of 1 means that the forecast for the current time step is based on the previous time step's error.

**Selecting p, d, q parameters**: Choosing the optimal values for p, d, and q typically involves:

1.  **Stationarity Check**: Determine the 'd' value by checking if the time series is stationary (e.g., using statistical tests like the Augmented Dickey-Fuller test) and differencing it until it becomes stationary.
2.  **ACF and PACF Plots**: Analyze the Autocorrelation Function (ACF) and Partial Autocorrelation Function (PACF) plots of the stationary series. These plots help identify the appropriate 'p' (from PACF) and 'q' (from ACF) values.

For this initial forecast, we will use `ARIMA(1,1,1)` as a starting point. It's important to note that this is an educated guess without detailed ACF/PACF analysis, and optimal performance would require further investigation into these parameters.
"""

from statsmodels.tsa.arima.model import ARIMA

# Set 'ds' as index
daily_referral_revenue_full_indexed = daily_referral_revenue_full.set_index('ds')

# Fit ARIMA(1,1,1) model (as a starting point)
# We'll use a simple order (1,1,1) for demonstration. For optimal results, p,d,q should be determined via ACF/PACF plots.
model_arima = ARIMA(daily_referral_revenue_full_indexed['y'], order=(1,1,1))
model_arima_fit = model_arima.fit()

print("ARIMA model fitted successfully.")
print(model_arima_fit.summary())

# Define the forecast period: January 13, 2026 to December 31, 2026
forecast_start_date = '2026-01-13'
forecast_end_date = '2026-12-31'

# Generate future dates for the forecast
future_dates = pd.date_range(start=forecast_start_date, end=forecast_end_date, freq='D')

# Make predictions using the fitted ARIMA model
# The start and end for predict refers to the index positions, or 'start'/'end' dates.
# 'start' is the first index position to forecast, 'end' is the last.
# We need to find the index of 'forecast_start_date' in our full time series.
start_index_forecast = daily_referral_revenue_full_indexed.index.get_loc(forecast_start_date)
end_index_forecast = daily_referral_revenue_full_indexed.index.get_loc(forecast_end_date)

# Get predictions and confidence intervals
forecast_results = model_arima_fit.get_prediction(start=start_index_forecast, end=end_index_forecast, dynamic=False)
forecast_values = forecast_results.predicted_mean
conf_int = forecast_results.conf_int()

# Create a DataFrame for the forecast results
forecast_arima_df = pd.DataFrame({
    'date': future_dates,
    'revenue': forecast_values.values,
    'revenue_lower': conf_int.iloc[:, 0].values,
    'revenue_upper': conf_int.iloc[:, 1].values
})

# Ensure revenue values are not negative (as revenue cannot be negative)
forecast_arima_df['revenue'] = forecast_arima_df['revenue'].apply(lambda x: max(0, x))
forecast_arima_df['revenue_lower'] = forecast_arima_df['revenue_lower'].apply(lambda x: max(0, x))
forecast_arima_df['revenue_upper'] = forecast_arima_df['revenue_upper'].apply(lambda x: max(0, x))

# Filter the forecast_arima_df to display only April 1 to December 31, 2026
forecast_filtered_display = forecast_arima_df[(forecast_arima_df['date'] >= '2026-04-01') &
                                                (forecast_arima_df['date'] <= '2026-12-31')]

print("Forecast for April to December 2026 (first 5 rows):")
display(forecast_filtered_display[['date', 'revenue', 'revenue_lower', 'revenue_upper']].head())
print("Forecast for April to December 2026 (last 5 rows):")
display(forecast_filtered_display[['date', 'revenue', 'revenue_lower', 'revenue_upper']].tail())

import matplotlib.pyplot as plt
import seaborn as sns

# Plot the historical data and the forecast
plt.figure(figsize=(14, 7))
sns.lineplot(x=daily_referral_revenue_full_indexed.index, y=daily_referral_revenue_full_indexed['y'], label='Historical Revenue', color='blue')
sns.lineplot(x=forecast_arima_df['date'], y=forecast_arima_df['revenue'], label='Forecasted Revenue', color='red', linestyle='--')
plt.fill_between(forecast_arima_df['date'], forecast_arima_df['revenue_lower'], forecast_arima_df['revenue_upper'], color='red', alpha=0.1, label='95% Confidence Interval')

plt.title('Referral Revenue Forecast (ARIMA)')
plt.xlabel('Date')
plt.ylabel('Revenue (IDR)')
plt.legend()
plt.grid(True)
plt.ticklabel_format(style='plain', axis='y') # Prevent scientific notation on y-axis
plt.xticks(rotation=45)
plt.tight_layout()
plt.show()

"""## Insight dan Rekomendasi Bisnis Berdasarkan Forecast Referral Revenue (ARIMA)

**Insight Utama:**

1.  **Tren Penurunan yang Konsisten dan Signifikan**: Grafik forecast dengan jelas menunjukkan tren penurunan yang konsisten dan berkelanjutan pada pendapatan dari *leads* Referral hingga akhir tahun 2026. Ini mengindikasikan bahwa sumber pendapatan dari referral akan terus menyusut secara drastis.
2.  **Penurunan Signifikan pada Tahun-tahun Sebelumnya**: Analisis historis menunjukkan penurunan yang sangat tajam pada pendapatan referral di tahun 2024 dan berlanjut di tahun 2025. Forecast ini mengkonfirmasi dan memproyeksikan kelanjutan tren negatif tersebut.
3.  **Ketidakpastian Tinggi (Lebar Rentang Keyakinan)**: Area abu-abu pada grafik forecast (rentang kepercayaan 95%) terlihat cukup lebar, bahkan menghasilkan nilai negatif pada batas bawah. Ini menunjukkan tingkat ketidakpastian yang sangat tinggi dalam proyeksi model. Meskipun kita telah mengkonversi nilai negatif menjadi nol, rentang asli model mengindikasikan bahwa revenue bisa sangat rendah atau bahkan mendekati nol. Ketidakstabilan ini juga disorot oleh peringatan model ARIMA tentang matriks kovarians yang singular, yang menyiratkan bahwa parameter model mungkin tidak stabil.
4.  **Implikasi Serius bagi Pertumbuhan Bisnis**: Dengan tren penurunan yang tajam dan ketidakpastian yang tinggi, segmen referral tidak dapat diandalkan sebagai pendorong pertumbuhan pendapatan di masa mendatang jika tidak ada intervensi besar.

**Rekomendasi Bisnis:**

1.  **Re-evaluasi dan Optimasi Program Referral Secara Menyeluruh**:
    *   **Identifikasi dan Perkuat *Referrer* Utama**: Fokus pada *referrer* yang terbukti menghasilkan *leads* berkualitas tinggi dan *closing* (misalnya, 'Referral RT'). Berikan insentif yang lebih menarik, dukungan khusus, dan komunikasi yang proaktif untuk mempertahankan dan meningkatkan kontribusi mereka.
    *   **Klarifikasi 'Unknown Referrer'**: Prioritaskan upaya untuk mengidentifikasi dan membangun hubungan dengan 'Unknown Referrer' yang berkinerja baik. Hilangnya informasi ini adalah kerugian besar dalam strategi referral.
    *   **Analisis Motivasi *Referrer***: Pahami apa yang memotivasi para *referrer* untuk merekomendasikan layanan Anda. Apakah itu insentif finansial, reputasi, atau kepuasan terhadap layanan? Sesuaikan program referral Anda berdasarkan temuan ini.

2.  **Perbaiki *Bottleneck* Konversi 'Kirim Proposal'**: Seperti yang disorot dalam analisis sebelumnya, tahap 'Kirim Proposal' adalah titik kritis bagi *leads* Referral. Tingkatkan upaya pada tahap ini:
    *   ***Follow-up* Proaktif dan Personalisasi**: Kembangkan strategi *follow-up* yang lebih terstruktur dan personal setelah proposal dikirim. Pastikan semua pertanyaan dan keberatan calon klien ditangani dengan cepat dan efektif.
    *   **Optimasi Konten Proposal**: Kaji ulang efektivitas proposal. Pastikan proposal menyoroti *value proposition* unik firma, menjelaskan manfaat secara jelas, dan disesuaikan dengan kebutuhan spesifik *leads* referral.
    *   **Insentif Waktu Terbatas**: Pertimbangkan untuk menawarkan insentif (misalnya, diskon kecil atau layanan tambahan gratis) yang memiliki batas waktu untuk mendorong keputusan *closing* lebih cepat.

3.  **Penguatan Kualitas Layanan untuk Mendorong Referral Organik**: Penurunan referral yang konsisten menunjukkan potensi masalah dalam kepuasan klien secara keseluruhan. Klien yang tidak puas cenderung tidak akan merekomendasikan layanan Anda.
    *   **Survei Kepuasan Klien Pasca-Layanan**: Lakukan survei kepuasan secara teratur untuk mengidentifikasi area yang perlu ditingkatkan dalam layanan Anda. Gunakan *feedback* ini untuk perbaikan berkelanjutan.
    *   **Program Apresiasi Klien**: Terapkan program yang mengapresiasi klien setia untuk mendorong mereka menjadi *advokat* brand Anda.

4.  **Diversifikasi Strategi Akuisisi *Leads***: Mengingat tren penurunan yang mengkhawatirkan pada *leads* Referral dan Repeat Order, firma harus secara serius mempertimbangkan diversifikasi sumber *leads*.
    *   **Investasi pada *New Prospect* dan *New Organic***: Tingkatkan upaya pemasaran dan akuisisi pada segmen *New Prospect* dan *New Organic* untuk menyeimbangkan ketergantungan pada referral yang menurun.
    *   **Eksplorasi Saluran Baru**: Jelajahi saluran pemasaran baru atau kemitraan strategis untuk menjangkau segmen pasar yang belum tergarap.

5.  **Optimasi Model ARIMA**: Peringatan tentang kovarians matriks singular menunjukkan bahwa model ARIMA yang digunakan mungkin tidak optimal. Lakukan analisis lebih lanjut:
    *   **Analisis ACF/PACF**: Lakukan analisis Autocorrelation Function (ACF) dan Partial Autocorrelation Function (PACF) pada data residual untuk menentukan orde `p` dan `q` yang lebih tepat.
    *   **Uji Stationaritas**: Pastikan bahwa data deret waktu telah distasionerkan dengan benar (*differencing* yang sesuai untuk parameter `d`).
    *   **Pertimbangkan Model Alternatif**: Jika ARIMA terus menunjukkan kinerja yang tidak stabil, pertimbangkan model deret waktu lain seperti SARIMA (untuk musiman yang lebih kompleks), Prophet (jika ada kalender liburan atau *event* eksternal), atau model berbasis *machine learning* untuk *time series*.

**Kesimpulan**: Forecast referral revenue ini adalah **sinyal merah** yang membutuhkan perhatian dan tindakan cepat. Firma harus proaktif dalam merevitalisasi program referral dan mendiversifikasi sumber pendapatan untuk memastikan pertumbuhan berkelanjutan di masa depan.
"""

# @title step_artifacts
num_fig = "1" # @param {type:"string"}
step = 'ModelOptimization'  # @param ["DataLoading", "DataExploration", "DataCleaning", "DataWrangling", "DataVisualization", "DataSplitting", "ModelDevelopment", "ModelOptimization", "ModelEvaluation", "Summary", "ReferralForecasting"] {type:"string"}
# Assuming `plt.gcf()` would get the current figure which is the forecast plot.
# If you have a specific figure object, pass that instead (e.g., `fig`).
# For this example, I'll assume plt.gcf() is appropriate after plt.show() is called implicitly.
# Note: This will capture the last shown plot. If there are multiple plots, ensure you call this after the specific plot you want to save.
# Placeholder for upload_plt_to_gcs function, assuming it's defined elsewhere.
# upload_plt_to_gcs(num_fig, step, plt.gcf())